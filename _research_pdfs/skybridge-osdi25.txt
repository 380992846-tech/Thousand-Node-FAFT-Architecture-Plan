                 Skybridge: Bounded Staleness
                    for Distributed Caches
   Robert Lyerly, Meta Platforms Inc.; Scott Pruett, unaffiliated; Kevin Doherty
        and Greg Rogers, Meta Platforms Inc.; Nathan Bronson, OpenAI;
                         John Hugg, Meta Platforms Inc.
              https://www.usenix.org/conference/osdi25/presentation/lyerly




This paper is included in the Proceedings of the 19th USENIX Symposium
          on Operating Systems Design and Implementation.
                         July 7–9, 2025 • Boston, MA, USA
                                ISBN 978-1-939133-47-2

            Open access to the Proceedings of the 19th USENIX Symposium
           on Operating Systems Design and Implementation is sponsored by
                           Skybridge: Bounded Staleness for Distributed Caches

             Robert Lyerly                     Scott Pruett∗       Kevin Doherty                     Greg Rogers
           Meta Platforms Inc.                                    Meta Platforms Inc.             Meta Platforms Inc.
                                           Nathan Bronson∗                John Hugg
                                               OpenAI                 Meta Platforms Inc.


                              Abstract                                  While this architecture is highly scalable, it means there are
                                                                     dozens of copies of millions of shards around the world that
Meta Platforms Inc. is a social media company whose prod-
                                                                     are kept up to date. Meta uses asynchronous replication [15]
ucts require high availability and low latency. Meta’s services
                                                                     to keep copies synchronized in order to meet our strict latency
run in multiple geographic locations around the world and
                                                                     and high scalability requirements. Asynchronous replication
use asynchronous replication to keep the numerous cached
                                                                     provides eventual consistency [6] for clients, meaning all up-
copies of the datastore in sync. This setup reduces consis-
                                                                     dates will eventually be reflected in all replicas but without
tency in order to meet availability and latency requirements.
                                                                     bounds on how long it may take. Eventual consistency is prob-
Eventual consistency due to asynchronous replication causes
                                                                     lematic when there is non-trivial replication lag that delays
issues for Meta’s services, ranging from minor annoyances to
                                                                     when updates are visible to clients. Due to the scale of Meta’s
product-breaking bugs. Therefore, we ask: can we put mean-
                                                                     serving stack (both in traffic and geographic size), it is almost
ingful bounds on how long it takes writes to be visible while
                                                                     impossible to eliminate all sources of lag. An overloaded
maintaining the scalability afforded by eventual consistency?
                                                                     publisher, a hotly-written shard or a congested network hop
   In this work we present Skybridge, an out-of-band replica-        can all cause replication lag for multiple shards and many
tion stream for providing bounded staleness for distributed          downstream replicas.
caches. Skybridge takes advantage of the fact that Meta’s
                                                                        Eventual consistency is a recurring pain point in many
systems already have a reliable delivery stream and instead
                                                                     previous works [2, 39, 57]. For Meta, eventual consistency
focuses on real-time delivery of updates. Skybridge is comple-
                                                                     causes bad user experiences in products. Visibility controls
mentary to the main replication pipeline and avoids correlated
                                                                     are a prime example – for instance, Alice may add Bob to
failures while being lightweight. We show that Skybridge
                                                                     a closed group. However if the group’s membership data is
helps provide 2-second bounded staleness for 99.99998% of
                                                                     read from a lagging replica, Bob will not be able to participate
writes, while the main replication pipeline only achieves this
                                                                     in the group until the replica catches up. This violates Alice
99.993% of the time. Skybridge is able to achieve this while
                                                                     and Bob’s user expectations and causes frustration with our
only being 0.54% the size of cache deployments.
                                                                     products. Eventual consistency can also have more serious
                                                                     side-effects. For example, Meta’s serving stack allow trigger-
1     Introduction                                                   ing asynchronous actions outside of web request handling
                                                                     to avoid blocking when returning a response. Asynchronous
Meta Platforms, Inc. is a social media company whose
                                                                     actions may read from different replicas or even execute in
products are used by people around the world. Like other
                                                                     different regions. In one instance a content enforcement sys-
web-scale serving stacks, Meta’s datastore hosts millions of
                                                                     tem using asynchronous actions experienced an outage due to
database shards and handles billions of queries per second
                                                                     sustained replication lag. The action could not see data writ-
at sub-second latency [11, 15, 19, 45, 63]. Because of these
                                                                     ten during the web request, leading to a surge of retries that
stringent requirements, Meta operates instances of the serving
                                                                     clogged up execution queues and caused system downtime.
stack in multiple geographic regions worldwide. Each region
stores numerous copies of the data in the form of database              Meta’s product developers are aware there is replication
(DB) replicas [19, 41, 44], in-memory caches [11, 22, 52, 63]        lag in the system, but without stronger guarantees they build
and indexing systems [9, 57]. This architecture provides high        workarounds to deal with distributed systems issues that leak
scalability and close proximity to users for low latency.            through datastore abstractions. Developers often ask “what
                                                                     is the maximum cross-region replication lag?” Infrastructure
    ∗ Work done while at Meta Platforms Inc.                         teams struggle to provide useful answers as lag can vary from



USENIX Association                               19th USENIX Symposium on Operating Systems Design and Implementation            187
                                                                     gap detection that focuses on detecting cache staleness,
                                                                   • Describe Skybridge’s design and implementation, a system
                                                                     that provides fine-grained and efficient bounded staleness
                                                                     for distributed in-memory caches,
                                                                   • Evaluate Skybridge’s ability to provide reliable bounded
                                                                     staleness for TAO users, including demonstrating that
                                                                     TAO’s 2-second consistency increases from 99.993% to
                                                                     99.99998% using Skybridge. Skybridge helps TAO prove
                                                                     data is fresh without having to refill from the primary source
                                                                     for 99.9996% of requests while only requiring 0.54% of
                                                                     TAO’s server footprint.

                                                                   2     Background
  Figure 1: System architecture for Meta’s serving stack.
                                                                   2.1    Meta’s system architecture
milliseconds to hours (or longer). Developers cope with lag        Meta’s serving stack is similar to other web serving stacks
via patterns like retry loops, racy state machines and buggy       like PNUTs [15] or Espresso [51]. The serving stack priori-
distributed locks. For example, one developer added a retry        tizes high availability and low latency, meaning Meta operates
loop that would poll the datastore for up to 40 minutes waiting    many deployments around the world.
for data. Better guarantees on when writes become visible             Storage. Meta stores its user data in MySQL [43]. The
obviate a whole class of developer issues. Thus, we ask how        database is partitioned into millions of shards and replicated
can we put reliable and tight bounds on how long it takes for      across regions. Meta uses primary-secondary replication [44]
a write to be visible while maintaining the serving stack’s        to keep regions up to date. For each shard, one region hosts
high scalability and low latency?                                  the primary replica that accepts both read and write requests.
   In this work we present Skybridge, an out-of-band replica-      Other regions host secondary replicas, which only accept read
tion system for providing 2-second bounds on eventual con-         requests. Writes to the primary are asynchronously replicated
sistency. Meta’s existing replication system, which provides       to secondaries, i.e., MySQL acknowledges completed writes
strong durability and ordering guarantees, already replicates      to the client before the change is guaranteed to have been
updates within 2 seconds in most cases. Skybridge instead          replicated to all secondaries.
targets the long-tail of lagging shards, where the strong guar-       Caching. Meta’s workload is skewed towards reads. This
antees of traditional replication systems cause replication lag.   workload is amenable to caching, which allows scaling
Our core insight is that Skybridge can implement replication       throughput with low latency. Meta operates an in-memory
with weaker guarantees while still enabling the system to          caching service named TAO [11] in every region. TAO is simi-
holistically enforce staleness bounds. The weaker semantic,        lar to Memcached [22] or Redis [52], but provides a graph [67]
named replication with gap detection, allows out-of-order          API for querying vertices and edges. TAO is separated into
replication and known data loss. These properties allow Sky-       read and write tiers, which allows specialization (DB con-
bridge to side-step common sources of replication lag and          nection management for writers, high-throughput caching for
focus on real-time replication, complementing the existing         readers) and independent scaling of resources. TAO uses a
replication stream. While Skybridge builds upon existing dis-      read- and write-through architecture. For writes, web servers
tributed systems mechanisms, it combines them to provide a         issue requests to the in-region TAO write tier. If the primary
new replication guarantee and occupies a novel point in the        replica for the shard is in-region, the write tier directly issues
replication system design space.                                   a request to MySQL; otherwise, the request is proxied to the
   Skybridge is designed to be used by in-memory distributed       write tier in the primary region to perform the write. Web
caches, including TAO [11], the cache fronting Meta’s serving      servers similarly send read requests to the local TAO read
stack. When the replication stream is lagging, TAO consults        tier. On cache misses, TAO goes “upstream” to fetch the data.
Skybridge to determine which individual cache items are stale      Figure 1 shows the upstream path – TAO first attempts to
and need refilling from the source of truth. Skybridge’s protec-   obtain the data from the local replica (1), and if unsuccessful,
tions are enabled by default for all TAO requests, transparently   attempts to obtain the data from the read tier in the primary
boosting consistency with minimal overhead.                        region (2) before going to the primary DB replica (3).
   In this work we make the following contributions:                  Keeping caches up to date. Keeping caches up to date is
                                                                   a known difficult problem [49]. One strategy is to use time-
• Explain why bounded staleness is important for TAO’s users       to-live (TTL) caching [14]. However, TTLs cause caches
  and why we did not target other forms of consistency,            to either hold updated data too long (stale data) or throw
• Define a novel replication guarantee called replication with     away unchanged data (spurious misses). Another strategy is



188   19th USENIX Symposium on Operating Systems Design and Implementation                                    USENIX Association
to rely on read/write-through architectures to update caches       processed all updates Ti where Ti < T j . In essence,
on the request handling path. However, relying on a fixed          TAO is guaranteed to be “up to date” as of the watermark.
topology for consistency prevents evolving the architecture
for operational benefits like availability and scalability [57].   2.2    Bounded staleness
   Instead, Meta uses a pub-sub system named Worm-                 Bounded staleness [40] is a read consistency level exposed by
hole [56], a system similar to Apache Kafka [3] or LinkedIn’s      asynchronously-replicated datastores. Unlike eventual con-
Databus [18], to stream updates to caches. Wormhole ingests        sistency, which specifies that reads to the datastore will ob-
MySQL’s replication stream and distributes updates to sub-         serve a write at some point without any conditions on time-
scribers such as TAO read tiers. Wormhole guarantees at-           liness, bounded staleness guarantees that reads will observe
least-once and in-order delivery of updates to subscribers.        any write within some system-provided time bound. Con-
While Wormhole’s strong guarantees ensure the system can           cretely, if write WT is performed at time T and the system
reliably replicate data around the world, enforcing those guar-    provides staleness bound S, then any read R performed at or
antees often leads to replication lag. For example, a hot shard    after T + S is guaranteed to observe WT regardless of whether
or an overloaded publisher will cause lag (and data staleness)     the read is to the primary or a replica. Note that under bounded
on a large number of subscribers. While average replication        staleness the reader is allowed to observe subsequent writes
lag is low, Wormhole provides no bounds on maximum lag.            WT +1 ,WT +2 ... (i.e., fresher data) but reads must reflect all
Because both the MySQL secondaries and caches are updated          older writes WT −1 , ... that preceded WT .
asynchronously with no bound on replication lag, users are            Why bounded staleness? Bounded staleness is strong
subject to eventual consistency with unbounded staleness [6].      enough to be useful for developers while still being scalable
   Load-based subscriptions. There are multiples types of          enough to be enabled by default for all TAO reads. Bounded
TAO tiers per region, including main and “failover” tiers.         staleness matches most users’ mental model of the system. En-
Failover tiers help improve availability and handle traffic dur-   abling it by default provides highly leveraged benefits for all
ing maintenance events. Failover tiers lazily subscribe to shard   of Meta’s developers, including reducing product raciness and
replication streams when a shard’s traffic reaches a minimum       eliminating debugging pain. Others have recognized bounded
threshold. These load-based subscriptions reduce resource          staleness as a powerful consistency level for users – for ex-
usage but are frequent sources of staleness.                       ample, Azure Cosmos DB [41] and Spanner [23] support
   Write versioning. Meta’s datastores use hybrid-logical          bounded staleness (although they target DB replicas, not the
clocks (HLC) [32] as a building block for consistency primi-       caches fronting them).
tives. HLCs are versioning metadata about writes that have            Stronger forms of consistency like linearizability [27], se-
both a physical and logical clock component. HLCs achieve          quential consistency [36] and causal consistency [1] are non-
properties of both types of clocks – they capture causality        starters as defaults at Meta’s scale as they introduce too
between events (like Lamport clocks [35]) while also being         much communication and synchronization overhead. Previ-
tied to the physical time when the write was performed. HLCs       ously, Meta built and deployed FlightTracker [57] to provide
can be used to detect when a data item is stale and needs to       read-your-writes (RYW) consistency for Meta’s social graph.
be refreshed from a more up-to-date source.                        While RYW is enabled for all TAO reads, we observed several
   Meta’s MySQL deployment mints HLCs as part of com-              limitations that made us turn to bounded staleness. First, users
mitting transactions. MySQL guarantees HLCs are mono-              are only provided guarantees about their own writes; they still
tonically increasing per shard using the formula HLCnew =          get eventual consistency for writes performed by other users or
max(Timenow , HLCPrev + 1) (where Timenow is the host’s cur-       non-users like automation. Second, RYW protections are only
rent physical time). Meta uses NTP [42] to synchronize phys-       provided by default within a region, as adding cross-region
ical time. HLCs propagate alongside changed data through           latency on every session fetch adds too much overhead to be
Wormhole to TAO, which caches them alongside the data.             enabled by default [57]. Finally, integrating a FlightTracker-
Additionally, MySQL injects a heartbeat [31] message (with         like system in the long tail of products and services operated
HLC) into the replication stream every 500ms. This allows          by Meta is time consuming and not scalable.
downstream subscribers to disambiguate between inactive               We have found that RYW and bounded staleness together
shards and unhealthy replication – a subscriber should at least    form a compelling semantic for datastore users – a user’s
see a heartbeat every 500ms, regardless of whether there are       writes are visible to them immediately and all other writes are
any writes to the shard. If the subscriber does not see one, the   visible within a reasonable delay.
replication stream may be unhealthy.                                  Picking a staleness bound. The target staleness bound
   TAO tracks per-shard heartbeats as monotonically increas-       must both be useful for users and be achievable considering
ing watermarks indicating the shard’s replication progress.        Meta’s global deployment. Setting the bound to 2 seconds
Because Wormhole guarantees in-order delivery, when TAO            struck the right compromise between usability and feasibility
processes a heartbeat with HLC T j , TAO is guaranteed to have     – it is both a useful human-scale time bound and is achiev-
                                                                   able given speed-of-light delays for cross region replication.



USENIX Association                           19th USENIX Symposium on Operating Systems Design and Implementation             189
We evaluate Skybridge’s ability to reliably provide 2 second                   this information, TAO can figure out whether the cache is
bounded staleness in Section 5.                                                up to date or whether it needs to refill from upstream. TAO,
   Bounded staleness without Skybridge. TAO in principle                       Wormhole and Skybridge work together to enforce 2-second
can implement a simple form of bounded staleness on cache                      bounded staleness by default for all reads.
hits by comparing the staleness bound S against the phys-                         Skybridge is complementary to the existing Wormhole
ical component of the watermark HLCwm and cached-item                          replication stream, helping make bounded staleness enforce-
HLCitem . When checking a cache item:                                          ment precise and scalable. Wormhole is able to replicate most
                                                                               DB updates within 2 seconds. Skybridge provides the most
    1. If Timenow − (S − ε) < max(HLCwm , HLCitem ) (where ε                   benefit for the problematic long tail of lagging shards, where
       is max NTP clock skew1 ) then the cache item is fresh                   Wormhole’s strong replication guarantees cause lag. Sky-
    2. Otherwise, the cache item is stale and needs to be refilled             bridge implements an independent replication pipeline and
                                                                               is designed to avoid the same sources of replication lag. Sky-
   Note that TAO uses the max of the watermark and cache                       bridge achieves this by relaxing its replication guarantees,
item HLC. In some instances, the cache item HLC may be                         which allows it to focus on real-time replication instead of
older than the staleness bound. This does not necessarily mean                 durability and in-order delivery.
that the item is stale – it may simply have not been written                      Skybridge has several other goals in order to support
recently. The replication watermark helps TAO prove these                      bounded staleness checking for TAO’s workload. Checking
cache items as up to date.                                                     against Skybridge must be low latency, as TAO cannot spend
   This condition can be checked upstream all the way to the                   significant time doing expensive bounded staleness checks
primary DB, where the data is guaranteed to be fresh. This                     that regress request latency for clients. Additionally Skybridge
check is local to each cache host, meaning it adds minimal                     must prevent as many spurious upstream fills as possible, both
overhead to reads. However, this baseline implementation                       to avoid cross-region latency and to avoid overloading the
causes significant operational issues. It is not fine-grained – if             upstream path. This means that the vast majority of bounded
the shard temporarily lags beyond the staleness bound, TAO                     staleness checks must be satisfied locally on TAO hosts. Fi-
will treat all data on the shard as stale and go upstream to guar-             nally, Skybridge should ideally require significantly fewer
antee freshness (even though all data has likely not changed).                 resources than either TAO or Wormhole so that it can be
This degrades TAO into a proxy rather than a cache. It can                     efficiently deployed alongside existing systems.
also add significant latency as requests that should be served                    We first describe Skybridge’s replication semantics. Then,
locally may be forced to go cross-region. Finally, it creates                  we describe Skybridge’s architecture and how it implements
thundering herds [37] on the primary, adding additional DB                     this semantic to enforce bounded staleness.
load and potentially exacerbating replication lag.
   Importantly, almost all cache misses due to staleness in this               3.1    Replication with gap detection
implementation are false positives. Because Meta’s workload
is highly read-skewed, most data on a shard has not been                       Skybridge implements a replication semantic called replica-
recently written and is actually up to date. The problem is                    tion with gap detection (RGD). Systems that implement RGD
TAO does not have fine-grained information about which                         have several unique properties that avoid common sources of
particular cache items have changed since the last replication                 replication lag while still enabling caches like TAO to enforce
watermark. What TAO needs is a staleness oracle to identify                    bounded staleness.
which cache items have been recently written. When the                            Replicating write metadata. The first property relates
replication stream is lagging, TAO could check the oracle to                   to who is responsible for filling stale cache entries. RGD
see whether a cache item has recently changed. This would                      systems only focus on identifying which cache items are
allow TAO to filter out spurious staleness misses and only                     stale. They leave the responsibility of refilling cache to others,
refill data that is truly stale.                                               e.g., TAO’s upstream fills. RGD systems only need the write
                                                                               metadata, i.e., cache key (node or edge ID) and DB HLC
3     Design                                                                   minted by the DB, to identify stale cache entries. Note that
                                                                               if RGD systems wanted to instead fill cache, they would
Skybridge’s goal is to be a fine-grained staleness oracle for                  need to replicate the changed data in addition to metadata.
TAO. When TAO receives a read request for a cache item and                     This would require significantly more CPU, memory and
the replication stream backing that item is lagging, TAO con-                  network resources. Additionally, this effort would be mostly
sults Skybridge to determine if the item has recently changed.                 wasted – recall from Section 2.2 that most cache items are
Skybridge responds with HLCs of writes to that item, if any;                   up to date even if the backing replication stream is lagging.
usually, however, it responds there are no recent writes. With                 Only a small number of reads would actually use the changed
    1 If maximum clock skew exceeds ε, TAO may silently violate bounded        data. It is more resource efficient to instead focus only on
staleness and return stale data to users. We describe how Skybridge accounts   replicating write metadata; we evaluate the resource savings
for ε in Section 4.                                                            in Section 5.3.



190     19th USENIX Symposium on Operating Systems Design and Implementation                                              USENIX Association
                                                                       At read time, clients query the RGD system by key. If
                                                                    there are multiple <key, HLC> tuples in the set, the writes are
                                                                    merged using a last-write-wins strategy [50]. This is because
                                                                    the RGD system’s only responsibility is to give the cache
                                                                    the versioning information required to produce a bounded
                                                                    staleness-compliant refill. Only the latest HLC matters – all
                                                                    previous writes will be reflected in the latest write. The RGD
                                                                    system returns the max of all found HLCs in order to make
                                                                    sure the cache observes the latest update.
                                                                       Putting it all together. These three properties allow Sky-
                                                                    bridge to focus on replicating metadata about the most re-
                                                                    cent writes while de-prioritizing or even dropping older data.
                                                                    Skybridge’s replication stream is significantly smaller than
Figure 2: Cross-region replication architecture as (1) Sky-         traditional replication streams and side-steps many of the
bridge ingests writes as they flow through TAO, (2) Skybridge       traditional sources of replication lag. Because of these prop-
replicates metadata across regions, and (3) Skybridge indexes       erties, Skybridge can focus on real-time replication of write
metadata for reading from TAO.                                      metadata in under two seconds.

                                                                    3.2    Skybridge architecture
    Known data loss. The second property is that RGD allows         Figure 2 shows an overview of Skybridge’s architecture. Sky-
data loss as long as it is detectable. When using an RGD            bridge has three main components: 1) an integration with
system to check staleness, caches expect a “yes, write at HLC       TAO’s write tier to ingest write metadata about all DB writes,
X” or “no recent writes” answer. However if the RGD system          2) a replication layer to fan-out write metadata to all read
reports an “indeterminate” result (i.e., it is missing data), the   regions, and 3) an integration with TAO’s read tier to check
cache can always conservatively assume it is stale and refill       which cache items are stale.
the entry. While the cache may have been unnecessarily re-             Skybridge’s write path (Section 3.4) ingests write metadata
filled, the system as a whole still enforces bounded staleness.     from TAO writers and detects gaps in the ingestion. Sky-
The system is correct as long as the RGD system can reliably        bridge uses leases, which indicate when a TAO writer may
detect when it is missing data. If, however, the RGD system         write to a shard, to determine data completeness. Skybridge’s
cannot detect when it is missing data, it may falsely report        client inside of TAO writers is responsible for opening leases
“no recent writes” to the cache. The cache would silently re-       with a system called Skylease and batching writes to send to
turn stale results to clients in violation of bounded staleness.    Skybridge. Skybridge’s write path receives these batches and
Therefore the RGD system must be able to detect these “gaps”        reads lease holders from Skylease to determine when it has
in order to provide bounded staleness. Note that this property      complete data and is therefore safe to replicate downstream.
follows directly from the first – data loss is allowable because       Skybridge’s replication layer (Section 3.5) is responsible
the RGD system is not responsible for filling cache (the cache      for real-time replication of write metadata from the write
provides its own refill mechanism).                                 path to the read path. Skybridge uses a pull-based model for
    Out-of-order replication. The third property is that RGD        replication – Skybridge readers pull batches of writes from the
allows out-of-order replication. This is possible because the       write path. This gives readers the flexibility to prioritize what
data in the replication stream is a conflict-free replicated        data to fetch, including focusing on the newest metadata and
data type (CRDT) [55]. CRDTs are replicated data structures         de-prioritizing older data. The replication layer is designed to
whose replicas are guaranteed to converge to the same state         search for write metadata across multiple replicas, including
once all updates have been delivered, regardless of the deliv-      write-path hosts and in-region read-path peers. If a particular
ery ordering. This requires updates be idempotent, associative      window of data cannot be replicated, the replication layer will
and commutative. Because RGD systems implement a CRDT,              eventually give up and move on to newer data.
they can replicate data in whatever order is most effective.           Skybridge’s read-path (Section 3.3) indexes write metadata
    RGD replication streams are add-only set CRDTs [55]             about all recent writes to all shards performed globally. Sky-
containing write metadata represented by <key, HLC> tuples.         bridge’s indexes are designed for fast querying from TAO’s
Essentially RGD systems implement a set that only ever ac-          read handling request path. TAO readers implement bounded
cumulates knowledge of recent writes. The order in which            staleness checks by first comparing replication watermarks
write metadata is added does not matter – the set will have the     to the staleness threshold (Section 2.2). If the cache item can-
same state if write A is added before write B or vice-versa.        not be proven fresh, TAO calls into the Skybridge client for
Additionally, the set will de-duplicate write metadata added        fine-grained checking. Skybridge’s client sends requests to
more than once since the set already contains the tuple.            Skybridge to determine whether a cache item is stale. Addi-



USENIX Association                            19th USENIX Symposium on Operating Systems Design and Implementation              191
                                                                  Note that Skybridge is only checked by the first cache host –
                                                                  other upstream hosts simply forward the request upstream
                                                                  if their cache HLC is too old. It adds too much latency to
                                                                  re-check Skybridge on every hop.
                                                                      Bloom filters. TAO can only send getWrites requests to
                                                                  Skybridge after calculating cache HLCs, meaning Skybridge
      Figure 3: Request handling flow for TAO reads               requests are on the cache read request critical path. However,
                                                                  TAO can actually predict when it will have to start checking
                                                                  staleness with Skybridge by monitoring replication stream lag.
tionally the client preloads Skybridge’s indexes on the TAO
                                                                  If TAO can proactively load Skybridge’s indexes for lagging
host for lagging shards to minimize the number of required
                                                                  shards, it can perform staleness checks locally rather than
external requests. If Skybridge reports the item is stale or
                                                                  sending requests to Skybridge. The main downside of this
if Skybridge has missing data, TAO goes upstream to refill
                                                                  approach is that even though Skybridge’s indexes are small,
cache. Otherwise TAO serves the cache to clients.
                                                                  they are still large enough to compete with TAO’s caches for
                                                                  memory. Storing Skybridge’s indexes locally could regress
3.3    Read path
                                                                  TAO’s hit rate and cache request latency.
TAO interacts with Skybridge during handling of read re-              Instead, Skybridge uses bloom filters [58] built from its
quests. The main goal is to determine whether the item that       indexes. Bloom filters can have false positives, i.e., the bloom
TAO has in cache is fresh with respect to the 2-second stal-      filter may indicate the data has changed even when it actually
eness bound. TAO achieves this through a combination of           has not. The bloom filters do not, however, have false nega-
Wormhole watermarks, Skybridge requests and local caching         tives. This allows TAO to correctly enforce bounded staleness.
of Skybridge metadata. This checking is designed to be pre-       The set of recent writes on a shard is sparse, meaning the
cise and lightweight, limiting bounded staleness cache misses     generated bloom filters are minimal in size. TAO pre-loads
only to those cache items that have actually recently changed     bloom filters for lagging shards, helping prove a significant
and without adding significant cache request latency.             portion of read requests up to date without having to send
   Figure 3 shows TAO’s bounded staleness control flow.           a request to Skybridge. This also significantly reduces the
First, TAO checks whether the item is in cache; if it is a        request load on Skybridge. Note that Skybridge only sends
cache miss, bounded staleness checking is not required. Next,     bloom filters for complete windows; there is no reason to
TAO constructs a cache HLC, HLCcache from the max of              send them for incomplete windows since TAO would have to
the watermark and cache item HLC (Section 2.2). If the            consult Skybridge or go upstream anyway.
physical component of this HLC is newer than the staleness            Listing 1 shows Skybridge’s bloom filter streaming API.
bound, the item is fresh and TAO returns it to the caller. If     Skybridge chunks its indexes into discrete intervals and builds
not, TAO consults with Skybridge to determine whether the         bloom filters that are continuously streamed to clients. TAO
item was written between [HLCcache , StalenessBound], where       monitors replication lag across all shards and opens a bloom
StalenessBound = Timenow − (2 seconds − ε). This interval         filter stream for a shard when its replication watermark is
is referred to as the lag interval. Listing 1 shows the two       older than 1.5 seconds. This allows TAO to pre-load bloom
Skybridge APIs that TAO uses to check whether there were          filters for checking if lag increases beyond 2 seconds. When
any recent writes to the cache item within the lag interval.      the replication stream’s lag drops below 1.5 seconds, TAO
   Querying recent writes. Skybridge’s main API,                  closes the bloom filter stream.
getWrites, takes a cache item key and lag interval as                 Similarly to getTicket requests, TAO checks local bloom
arguments and returns the latest HLC of all DB writes to that     filters for writes within the lag interval. If TAO has bloom
key within that interval (TAO also supports range scans [47],     filters covering the entire lag interval and they indicate no
for which Skybridge returns the latest HLC for all keys in the    recent writes, TAO can prove it has fresh data and serve it
range). getWrites also indicates whether it had complete          from cache. Otherwise, TAO sends a getTicket request to
data for the interval (discussed further below). If Skybridge’s   Skybridge to get a definitive answer.
indexes were complete then TAO compares the cache HLC                 Skybridge server-side indexes. Skybridge’s indexes are
to the list of recent HLCs. If there are no recent writes or      sharded by DB shard and store write windows. A write win-
if the cache HLC is greater than any HLC in the list, then        dow is composed of a lower HLC bound, an upper HLC bound
TAO can serve the data. If Skybridge returns an HLC higher        and a list of write metadata for all writes to that shard per-
than the cache HLC, TAO sends an HLC-conditional request          formed in that write window. Skybridge hosts replicate meta-
upstream with that HLC; any upstream host that satisfies the      data from DB primary regions, where TAO hosts publish
HLC can fill the data. Finally, if Skybridge’s indexes are        metadata about writes they performed to the local Skybridge
incomplete or there was an error talking to Skybridge, TAO        tier. getTicket requests scan write windows overlapping the
refills from upstream using StalenessBound as the condition.      lag interval to search for any writes to the specified cache key.



192   19th USENIX Symposium on Operating Systems Design and Implementation                                  USENIX Association
1  struct Interval { i64 lower ; i64 upper ;};                               Leases. Leases are a contract between TAO writers and
2  struct Response {                                                      Skybridge. A writer is only allowed to write to a shard if it
 3   list < tuple < string , i64 >> hlcs ; bool complete ;};
                                                                          has an open lease and the host promises to send heartbeats
 4 struct BloomFilter {
 5   Interval interval ; binary serializedFilter ;};                      for the duration of the lease. If for whatever reason a lease
 6 // Read recent writes from Skybridge                                   holder fails to send a heartbeat (e.g., host crash), Skybridge
 7 Response getWrites( string key , Interval lag );                       will detect data loss by observing a gap in expected heartbeats.
 8 // Stream bloom filters from Skybridge
 9 stream < BloomFilter > openBloomFilterStream(
                                                                          This is the core of how Skybridge implements replication with
10   i64 dbShard , i64 start );                                           gap detection’s known data loss guarantee.
                                                                             Leases are denoted by a lease holder (e.g., TAO host),
    Listing 1: Skybridge read API. TAO can request recent                 lease object (e.g., DB shard) and HLC interval. Leases are
    writes for a key and interval or stream serialized bloom filters      non-exclusive since multiple TAO writers may be handling
    constructed from Skybridge’s indexes.                                 writes for a given DB shard for higher availability. Lease
                                                                          HLC ranges are minted by Skylease hosts based on their
      When responding to queries, Skybridge needs to determine
                                                                          physical clock and the lease duration requested by the client,
    whether it has complete knowledge of all writes to a shard (i.e.,
                                                                          i.e., [Now, Now + Duration). We describe why using physical
    no gaps). Given a lag interval Ilag , index interval Iindex (formed
                                                                          clocks does not violate correctness below.
    by the union of all write windows indexed by Skybridge), and
                                                                             Skylease must be 1) highly available to avoid blocking
    write window D, Skybridge has complete data if:
                                                                          write handling, 2) durable to avoid losing leases and causing
                                                                          unknown data loss, 3) strongly consistent so that Skybridge
    1. Ilag ⊆ Iindex (the lag interval is covered by write windows
                                                                          knows the exact set of lease holders at a given time, and 4)
       indexed by Skybridge)
                                                                          scalable to leases on millions of lease objects. To satisfy these
    2. For all D ∈ Ilag ∩ Iindex , D.complete = true (all indexed
                                                                          requirements, Skylease acts as a batching and sequencing con-
       windows covering the lag interval are complete)
                                                                          trol plane on top of strongly-consistent distributed consensus
                                                                          systems like Apache Zookeeper [60] or Delos [8].
       If both criteria are satisfied, Skybridge has complete in-
                                                                             Skylease employs a number of techniques to scale
    formation about every write to the shard for the lag interval
                                                                          heavyweight consensus rounds to millions of leases. First,
    and can provide definitive cache freshness answers to TAO.
                                                                          Skylease’s protocol uses delta compression [30]. Because
    Section 3.4 describes how Skybridge’s write-path integration
                                                                          a writer’s shards do not frequently change, Skylease’s pro-
    determines whether write windows are complete and Sec-
                                                                          tocol only records changes between subsequent open lease
    tion 3.5 describes how Skybridge drives replication of write
                                                                          requests. Second, Skylease scales out by using its own shard-
    windows from primary regions for every DB shard.
                                                                          ing, where lease objects are hashed to a Skylease shard and
                                                                          each Skylease shard is backed by a consensus group. Finally,
    3.4    Write path
                                                                          Skylease batches writes to a Skylease shard from multiple
    Skybridge’s goal on the write path is to ingest metadata about        writers to reduce the number of consensus rounds.
    all writes committed to the DB. Skybridge ingests write meta-            Skybridge must handle the situation where reading lease
    data by collecting heartbeats from TAO write hosts. TAO               holders races with writers opening leases. For example, if a
    writer heartbeats are defined by a lower and upper HLC bound          TAO writer is allowed to open a lease on a shard after Sky-
    and a list of writes committed by that TAO writer within those        bridge has read the lease holders, Skybridge could experience
    bounds. TAO writers guarantee that heartbeats sent to Sky-            unknown data loss by missing a lease holder and therefore
    bridge contain all writes to the DB performed by that writer          missing heartbeats. To prevent this situation, Skylease peri-
    during that interval. Skybridge hosts aggregate heartbeats            odically seals leases on a shard by bumping a monotonically
    from TAO writers into write windows to publish downstream.            increasing watermark as part of consensus rounds. This wa-
       Skybridge uses a leasing system called Skylease to track           termark represents the lowest valid HLC of any newly opened
    which hosts are writing to which shards. Any single TAO               leases. If a TAO writer tries to mint a new lease with an inter-
    writer only serves traffic for a small fraction of DB shards.         val below or overlapping the watermark, Skylease rejects the
    Leases [24], which indicate the time period when a writer             lease by sequencing the operation through storage. Skylease
    may be writing to a shard, make heartbeating efficient by only        also responds to Skybridge queries (e.g., “read lease holders
    requiring TAO writers to send heartbeats for shards they own          for shard S between [Tx ,Ty )”) with “not sealed” until the inter-
    (i.e., avoid sending heartbeats from the cross-product of TAO         val is completely sealed. This ensures Skybridge sees the full
    writers and DB shards). Skybridge’s client is responsible for         lease holder set when building write windows.
    opening leases on hosted shards and batching write metadata              Skylease maintains an in-memory view of lease holders,
    into heartbeats to send to Skybridge. Skybridge must collect          including materializing the current watermark and full set
    heartbeats from all of a shard’s lease holders, i.e., writers, in     of TAO leases from delta compressed storage. Read queries
    order to have complete data for that shard.                           against this view are fast enough to allow frequent polling by



    USENIX Association                             19th USENIX Symposium on Operating Systems Design and Implementation                193
                                                                 Skylease host physical times - the HLC bounds will ensure
                                                                 the write falls into the appropriate time range.
                                                                    In order to build complete heartbeats, Skybridge’s client 1)
                                                                 maintains HLC bounds for DB write requests to avoid mint-
                                                                 ing new write HLCs outside of leases or already-sent heart-
                                                                 beats and 2) tracks outstanding writes (including in which
                                                                 heartbeats a write may land). The Skybridge write client
                                                                 achieves both goals using an in flight write storage for track-
                                                                 ing heartbeats and writes. This data structure maintains a list
                                                                 of heartbeats to be sent to Skybridge for each DB shard. Each
                                                                 heartbeat in storage tracks how many in-progress writes that
                                                                 could land within that heartbeat’s bounds. This storage en-
                                                                 sures heartbeats are complete before being sent to Skybridge.
                                                                    Figure 4 shows how the client uses this storage. (1) shows
Figure 4: Skybridge’s client maintains heartbeats (shown as      heartbeats getting added to storage based on leases opened
boxes) sorted from old to new with counts of in-flight writes.   with Skylease. Each lease gets divided into smaller discrete
(1) Leases opened with Skylease are divided into heartbeats      heartbeats since lease durations are much longer than indi-
and added to storage. (2) Before writing to the DB, TAO          vidual heartbeats. The heartbeats in storage are what TAO
acquires and creates HLC bounds from heartbeats. (3) The         promised to send to Skybridge but has not yet sent. Any inter-
client asynchronously scans for heartbeats to publish that are   val outside of storage either does not correspond to an open
older than a threshold and do not have any in-flight writes.     lease or represents a heartbeat that has already been sent to
(4) After the write is committed, TAO releases heartbeats and    Skybridge. In other words, future writes to be committed by
stores the write in the appropriate heartbeat (check mark).      the DB must land in one of the heartbeats in storage.
                                                                    (2) shows a write request coordinating with the storage
Skybridge until the queried interval is sealed. Skylease main-   before going to the DB. The client generates HLC bounds
tains watermarks per Skylease shard; watermarks apply to all     for the transaction based on heartbeats in storage. To track
lease objects on the shard. When advancing the seal water-       outstanding writes, the client acquires heartbeats in which
mark, Skylease keeps the new watermark behind the current        the write could land. This operation (maintained as a refer-
physical time to avoid impacting open lease availability.        ence count on the heartbeat) blocks the heartbeat from being
   Building heartbeats. Leases ensure Skybridge is able          published until the write has been committed by the DB. (4)
to determine which writers should be sending heartbeats          shows the post-DB processing – all heartbeats acquired in
for which shards. TAO writers are responsible for sending        (2) are released and the completed write’s metadata is stored
complete heartbeats for the entire opened lease duration         in the heartbeat containing its HLC. If there was an error
to Skybridge. A heartbeat H is complete if all writes W          writing to the DB, the heartbeats are simply released without
committed by the writer between the heartbeat’s lower and        storing any metadata. While the client could acquire every
upper HLC bounds are contained in the heartbeat’s write set:     single heartbeat in storage, most writes have HLCs that fall
                                                                 within a narrow range of heartbeats near the current physical
For all W such that Hlower ≤ W.hlc < Hupper ,W ⊆ H.writes        time. The client is configured to only acquire a small subset
                                                                 of heartbeats, which also helps prevent straggler writes from
Note that this implies that once a heartbeat is sent to          blocking large numbers of heartbeats from being published.
Skybridge, no more writes may be committed within that              (3) shows how the client determines when heartbeats are
heartbeat’s bounds so as to avoid violating the completeness     ready to be published to Skybridge. The client periodically
property for the sent heartbeat.                                 scans all shards in storage looking for heartbeats that are older
  TAO writers must only write to the DB within valid lease       than a publish threshold (which lags the host’s current physi-
bounds to ensure that Skybridge correctly tracks all writers.    cal time) and that do not have any outstanding writes. If there
To achieve this, TAO writers call into the Skybridge client      are still writes in flight that may land in a particular heartbeat,
before writing to the DB in order to read the current lease or   the client skips it. Otherwise the heartbeat is dequeued from
block until one is acquired. TAO then attaches the lease HLC     storage and sent to Skybridge. At this point the heartbeat is
bounds to the DB request. Meta’s MySQL deployment allows         sealed and all future writes will be prevented from landing
specifying HLC bounds on transactions – if the HLC minted        in it through the HLC bounds created in (2). Note that the
by the DB for a transaction does not fall within the bounds,     client takes advantage of out-of-order publishing to avoid
MySQL will abort the transaction. This ensures MySQL re-         delaying the sending of heartbeats. The client can send any
spects the leases opened by TAO writers. This HLC coordi-        heartbeat once it is ready for publishing, even if there are
nation with the DB is why it is okay to mint leases based on     older heartbeats blocked due to straggler writes.



194   19th USENIX Symposium on Operating Systems Design and Implementation                                  USENIX Association
   Tuning lease and HLC bound sizes. Lease durations and           windows from publishers to fill their indexes. Using a pull
HLC bounds represent a trade-off between Skylease load             model gives subscribers the ability to quickly detect gaps
and “blast radius” for when things go wrong. Lease duration        and re-fetch windows from another source. Because write
should ideally be as low as possible, but is bounded by how        windows are conflict-free replicated data types, subscribers
quickly Skylease can run consensus rounds. Alternatively,          have a lot of flexibility to reorder and retry data fetches. Using
TAO could open very long leases, e.g., on the order of min-        a push model would require publishers to discover missing
utes, to minimize Skylease load. However if the TAO host           data from subscribers, requiring too much coordination.
crashed shortly after opening a long lease, Skybridge would           The replication layer builds a continuous stream of write
have minutes of data incompleteness. Similarly, for heart-         windows with newer windows sent first. Early versions of
beats acquired before going to the DB, the bounds should be        the replication layer opened a single long-lived stream per
as small as possible to minimize the number of heartbeats          shard using a protocol similar to gRPC streams [25]. How-
blocked on stragglers, but large enough to give the DB ample       ever Skybridge frequently experienced head-of-line blocking
time to process the write (too small HLC bounds will cause         and could not prioritize newer data while the stream tried to
spurious aborts). The Skybridge client is tuned to open leases     flush out older windows. Additionally, long-running streams
on the order of tens of seconds and acquire HLC bounds on          caused load imbalance over time as Skybridge deployments
the order of hundreds of milliseconds.                             rotated hosts through maintenance. Long-lived publishers ac-
   Aggregating heartbeats. Skybridge aggregates heartbeats         cumulated more streams and eventually became overloaded
from in-region writers into write windows for a shard by           until they were restarted (and streams moved to other hosts).
1) polling Skylease for the (sealed) list of all lease holders,       Instead, Skybridge’s replication layer opens short seconds-
and 2) waiting for heartbeats from TAO writers covering those      long streams. This gives more load balancing opportunities
leases. Skybridge knows it has a complete write window D           across publishers and allows subscribers to de-prioritize older
if for Skylease watermark Swatermark and heartbeats H corre-       sections of the stream by either delaying re-fetching them
sponding to lease L:                                               or not re-fetching at all. Subscribers periodically open new
                                                                   streams to primary regions to stream the most recent data.
1. Dupper < Swatermark (Skylease has sealed the lease holder
                                                                   Publishers may send write windows out-of-order, publish
   set past the write window’s bounds)
                                                                   duplicate or incomplete windows or even leave gaps. None
2. For all L such that Dlower ≤ Llower < Lupper ≤ Dupper , HL ⊆
                                                                   of these block subscribers from continuing to stream newer
   D (Skybridge has all heartbeats from all lease holders for
                                                                   data. However when a subscriber detects missing data (e.g.,
   the write window interval)
                                                                   an incomplete write window), it schedules another attempt to
   Once Skybridge has all heartbeats from all lease holders for    stream the data after some delay. This helps when heartbeats
a given shard and interval, it builds a complete write window      from TAO are delayed, e.g., blocked waiting on a DB write.
ready for publishing. Skybridge is configured with a timeout       Additionally this allows subscribers to try re-fetching from
on how long it will wait for heartbeats. If the timeout expires    other sources. Each primary region contains two Skybridge
and it still has not received all heartbeats, Skybridge will       replicas, meaning there are two possible sources of writes. In-
publish an incomplete write window to give subscribers some        region replicas (“peers”) will also exchange write windows,
information about recent writes. Skybridge will, however,          which helps quickly warm up Skybridge hosts after a restart.
continue waiting and if the heartbeats later arrive it will re-       Finding primary regions. Subscribers need to know each
publish the complete window downstream. Skybridge again            shard’s primary region in order to fetch that shard’s writes.
takes advantage of out-of-order replication to avoid blocking      Similarly to TAO’s write path, Skybridge uses leases at the
complete windows from replication if an older window is            global level to track each shard’s primary region. For Sky-
waiting for data from TAO.                                         bridge to guarantee completeness, regional leases must only
                                                                   be opened within a global lease and therefore the region must
                                                                   have a global lease before any regional leases can be opened.
3.5    Cross-region replication                                    When handling an open lease request from TAO, regional
Skybridge’s replication layer is responsible for replicating       Skylease hosts implicitly open global leases for the DB shard
write windows from all primary regions to regions serving          and region with Global Skylease. Global leases are typically
read traffic within 2 seconds. While Skybridge’s correctness       opened for much longer intervals (e.g., on the order of min-
criteria allows gaps, Skybridge tries to replicate complete data   utes) as shards rarely move between regions.
as frequently as possible to be effective on TAO’s read path.         Subscribers drive replication for a shard by reading global
Because of the real-time nature of replication, Skybridge’s        leases from Global Skylease. The replication layer uses the
replication layer fetches missing data out of order and from       region name (i.e., lease holder) and lease bounds to stream
multiple replicas using a publisher-subscriber [3] model.          data from the appropriate primary region.
   While TAO writers push windows to the local Skybridge
tier, cross-region subscribers pull a continuous stream of write



USENIX Association                           19th USENIX Symposium on Operating Systems Design and Implementation               195
4     Implementation                                                bounded staleness reads, however, TAO has a lightweight
                                                                    drop-in replacement. When clients call this replacement API,
Skybridge provides a means for TAO to enforce bounded               the web server sleeps for 2 seconds to wait out the staleness
staleness on read requests. Below we discuss a number of            bound and then sends a fail-closed request to TAO. TAO either
practical considerations regarding TAO and Skybridge.               guarantees consistency or returns an error to the client if it
   Circuit breakers. Although TAO aims to provide bounded           cannot (e.g., hit upstream rate limits or network errors). This
staleness for all requests, Skybridge cannot regress TAO’s          means clients get linearizable reads. Similarly, bounded stale-
availability and performance. On the write path, too many           ness can provide a limited form of causal reads. If the client
writes blocked waiting for leases from Skylease will trip           knows when a write occurred (i.e., HLC of the write) then
a circuit breaker and TAO will let writes proceed without           it can wait until 2 seconds have passed to perform the read.
leases. Similarly, too many read requests trying to go through      TAO and Skybridge will ensure the write is visible. These
the high-overhead portion of bounded staleness checking (re-        APIs are highly efficient from TAO’s perspective since the
quests to Skybridge, consistency misses upstream) will trip         client waits for replication and TAO likely serves data from
a circuit breaker and TAO will serve whatever is in cache.          cache.
These circuit breakers prevent cascading failure modes. For            Clients can also use these APIs as a stronger backup when
example, if there is widespread Wormhole lag, bounded stale-        regular reads are not able to see a relevant write. Clients first
ness checking can create a thundering herd on the upstream          issue a regular read, which succeeds most of the time. On
path towards the DB which could make lag worse.                     the infrequent occasion that TAO is stale (e.g., clients do
   TAO “fails open” when tripping circuit breakers. TAO fires       not see an object they created), they wait for 2 seconds then
an alarm whenever these rate limits are hit to notify Meta          retry the read with stronger consistency. Bounded staleness
engineers about issues that need investigation. In fact these       enables these new APIs without any external synchronization
alerts have notified us about Wormhole and TAO problems             or forcing clients to pass around HLCs.
before either of the teams knew there was an issue.                    Failure scenarios and fault tolerance. Like most dis-
   Fail-closed vs. fail-open While TAO defaults to failing          tributed systems, Skybridge must contend with single-node
open if it hits rate limits, sometimes product developers want      failures. However, because of replication with gap detection
stronger consistency at the expense of availability – they          semantics, individual Skybridge host failures do not cascade
would prefer the request failed if TAO cannot guarantee             into replication lag or other outages. Skybridge’s replication
bounded staleness. TAO provides a “fail-closed” API for this        layer is resilient to these issues. There are multiple Skybridge
scenario. Clients opt in to the API by setting a flag on the TAO    write-path hosts per DB replica from which downstream sub-
request that tells TAO to fail the request if it cannot guarantee   scribers can stream updates. Similarly, there are multiple
bounded staleness. TAO reserves a portion of its rate limit         Skybridge read-path hosts per DB replica from which TAO
budget for fail-closed requests to prioritize their availability.   readers can open bloom filter streams and query for recent
   Extended retention Skybridge is an in-memory service             writes. Skybridge’s replication layer is free to stream updates
and is memory-capacity bound (see Section 5.3). Skybridge           from multiple sources in whatever order is best.
adjusts retention of its indexes to keep memory usage stable           While network partitions between TAO and Skybridge are
as write traffic fluctuates over time. Skybridge maintains a        technically feasible, we have not noticed them. Region drains
baseline retention for all DB shards, but TAO shard lag is          and cuts are more likely in practice, especially as part of
not uniform – individual shards may have worse lag than             routine testing [10]. However, Skybridge’s leasing system
others. Skybridge can selectively extend retention for a small      gracefully works around them. Recall that TAO writers (and
set of these longer-lagging shards. Skybridge detects these         Skybridge write-path hosts) are co-located in the same region
shards based on TAO queries – if it sees multiple requests for      as primary DB replicas. During a region cut, the region no
a shard where the lag interval is approaching retention limits,     longer receives any write requests. TAO writers no longer
Skybridge marks the shard as “extended” and maintains write         open leases and subscribers stop attempting to subscribe to
windows for that shard for a significantly longer interval.         updates from that region soon after. Therefore Skybridge,
   Clock skew Meta’s hosts use NTP [42] to synchronize              TAO and MySQL all share the same fate in a region outage
physical clocks. NTP is known to have on the order of hun-          and Skybridge in other regions still functions correctly.
dreds of milliseconds of clock skew. Meta is investigating us-         Skybridge’s read path has showcased several priority in-
ing more precise clock synchronization such as PTP [46, 54],        version [4] failures. A priority inversion exists when a higher
but for now we move the staleness bound forward by 50ms to          priority task is impacted by a lower priority task. For Sky-
try to account for clock skew.                                      bridge’s read path, this manifests as read traffic from TAO
   Emulating primary-only and causal reads TAO provides             affecting Skybridge’s replication health. This typically occurs
linearizable reads that bypass cache and read from the pri-         when Skybridge hosts have temporary spikes of incomplete
mary DB. To avoid DB overload issues, TAO actively discour-         data (e.g., a TAO writer crashing leading to a gap). In this
ages using them unless absolutely necessary. With fail-closed       scenario, Skybridge cannot send bloom filters to TAO since



196    19th USENIX Symposium on Operating Systems Design and Implementation                                   USENIX Association
                                                                   of DB shards to TAO writers rapidly changes. For example,
                                                                   in one instance a buggy TAO release caused write requests
                                                                   to get misrouted to TAO writers. Because TAO writers han-
                                                                   dle misrouted requests to maintain availability, they opened
                                                                   leases for the misrouted requests as well. Skylease’s delta
                                                                   compression was ineffective in this situation, leading to a
                                                                   spike in throughput to Skylease’s storage backend and out-of-
                                                                   memory crashes when materializing the set of lease holders.
                                                                   This caused excessive downstream incompleteness, leading to
                                                                   a large number of fail-open reads and writes. To prevent this
                                                                   situation, we added rate limits on how many lease changes
                                                                   can be sent per request. While this means there occasionally
                                                                   may be a brief spike in fail-open writes, it ensures Skylease is
Figure 5: Bounded staleness consistency over 7 days with no        still able to seal shards and Skybridge is able to publish write
bounded staleness (red), blind (blue) and fail-closed (green)      windows downstream in a timely manner.
reads. TAO’s 2-second consistency without Skybridge is
                                                                   5     Evaluation
99.993% but frequently drops below 99.985%. Skybridge
significantly increases consistency to 99.9993% for blind and      In this section we evaluate Skybridge’s ability to reliably and
99.99998% for fail-closed reads.                                   efficiently provide bounded staleness for TAO.
                                                                      Experimental setup. TAO runs in tens of regions around
it has incomplete data. This causes TAO hosts to fall back         the world, handling millions of write queries and billions of
to getWrites queries to Skybridge. getWrites queries are           read queries per second. For Skybridge to be effective on
more expensive than publishing bloom filters, especially since     TAO’s read path, it must be able to improve staleness for
bloom filters help TAO elide further getWrites calls. The          recently written data while filtering out almost all spurious
increase in getWrites traffic starves hosts of processing re-      staleness misses that would cause TAO to go upstream (and
sources to drive replication, leading to more data incomplete-     likely out of region). Below we describe Skybridge’s effec-
ness, fewer bloom filters and more getWrites traffic. Sky-         tiveness for Meta’s production workload.
bridge gets stuck in a metastable failure state [29] in which
the service is degraded until remediated by outside interven-      5.1    2-second staleness improvements
tion. To avoid this cycle, we added a priority queue for threads
in Skybridge hosts to prioritize replication, then bloom filter    We built a staleness checking service (referred to as the
streams and finally getWrites traffic. This ensures repli-         checker) to monitor 2-second consistency. TAO writers send a
cation and bloom filter streams are healthy, minimizing the        sample of writes to an in-region checker instance. On receiv-
number of getWrites calls from TAO.                                ing a write, the checker sleeps for 2 seconds from the write’s
                                                                   HLC and then issues read requests for that write to all TAO
   Similarly, Skybridge write-path hosts also experienced pri-
                                                                   tiers in all regions. The checker sends three types of requests:
ority inversion. Skybridge write-path hosts ingest heartbeats
from TAO writers and publish write windows downstream to           • No bounded staleness. The checker explicitly disables
subscribers. As mentioned in Section 3.5, the replication layer      bounded staleness protections. This is what users experi-
attempts to re-fetch missing windows. Many downstream sub-           ence without Skybridge.
scribers re-fetching data can compete with TAO writer heart-       • Blind. The checker issues a normal read request. TAO im-
beats, causing Skybridge write-path hosts to drop heartbeats.        plicitly applies bounded staleness protections but can fail
This causes data loss, leading to more subscribers attempting        open if it hits rate limits.
to re-fetch and further competition for resources. Skybridge       • Fail-closed. The checker issues a fail-closed request – TAO
again solves this problem using a priority queue to ensure           returns an error if it cannot guarantee bounded staleness.
heartbeat ingestion takes precedence over downstream sub-
scribers. Additionally, Skybridge write-path hosts rate limit         Figure 5 shows the difference in 2-second consistency ob-
the number of subscribers re-fetching windows.                     served by the checker over a 7 day period. By default, TAO
   Skylease demonstrated another interesting failure mode.         provides 99.993% 2-second consistency relying solely on
When Skylease is unhealthy and cannot provide leasing func-        Wormhole and frequently drops below 99.985%. With best-
tionality, TAO writers fail-open on write requests and handle      effort Skybridge, however, consistency jumps to 99.9993%,
writes without leases. Additionally, Skybridge is not able to      reducing the number of inconsistent reads without any user
read lease holders (since Skylease cannot seal shards), leading    intervention. The main limiting factor preventing better con-
to Skybridge publishing all of its write windows as incom-         sistency is TAO’s bounded staleness rate limits, which limit
plete. Skylease can become overloaded when the mapping             the number of Skybridge and upstream queries and causes



USENIX Association                           19th USENIX Symposium on Operating Systems Design and Implementation             197
      Fail-open reason             % of fail-open requests
       Long shard lag                       95.9%
    No replication stream                    3.3%
  Skybridge replication lag                  0.3%
 Missing data from write path                0.2%
   Skybridge request error                   0.2%

  Table 1: Fail-open reasons by % of all fail-open requests

the read to fail open. When the checker opts into fail-closed
mode however, the consistency rate climbs to 99.99998%.
This means only a couple thousand out of billions of requests
are stale after 2 seconds. At this scale, clock skew begins to
make a difference (recall staleness thresholds are created by a    Figure 6: Percent of read queries proven up to date using the
host’s physical time). Additionally the checker does not have      Wormhole watermark (red), host-local bloom filters (blue)
a way to opt the associated writes into fail-closed behavior       and an in-region request to Skybridge (green)
since it only observes writes after the fact, meaning it may be
checking writes that are not tracked by leases. Regardless, this   5.2    Traffic analysis
number has proven to be high enough to be used by products
                                                                   Skybridge helps TAO provide bounded staleness for all read
that require strong consistency guarantees for correctness.
                                                                   queries because it dramatically reduces the number of queries
   We further investigated the reasons for inconsistency in        that must be sent upstream to guarantee freshness. We eval-
the blind/fail open case. Table 1 shows fail-open reasons as       uated how frequently Skybridge is involved in TAO’s read
reported by TAO. The biggest cause of inconsistency was            handling and how effective Skybridge is in helping filter out
long shard lag. Because Skybridge is an in-memory service, it      upstream requests. Figure 6 shows the percentage of read
only maintains the most recent writes (see Section 5.3 below).     requests proven up to date solely based on the watermark (i.e.,
Skybridge’s retention cannot cover shards that are lagging         the Wormhole replication stream is not lagging), percent of
beyond several minutes, meaning TAO must fall back to going        up-to-date requests with the addition of Skybridge’s eagerly-
upstream. TAO hits upstream rate limits when a significant         streamed bloom filters (i.e., the bloom filter says there are no
number of shards lag by more than several minutes.                 recent changes), and percent of up-to-date requests with an
   The next biggest cause of inconsistency is reads to shards      authoritative request to Skybridge. Note that these numbers
without a replication stream, i.e., shards whose load was too      exclude requests to shards without a replication subscription,
low to trigger a subscription (Section 2). Without a subscrip-     i.e., load-based subscription.
tion, there is no replication watermark and the TAO host              Using Wormhole watermarks allows TAO to prove 99.96%
cannot check staleness (including using Skybridge) for that        of reads up to date. Checking against host-local bloom fil-
shard. The host must fall back to going upstream. Traffic on       ters increases that to 99.98%, meaning tens of millions of
TAO failover tiers frequently hit these rate limits.               additional read queries per second can be proven up to date.
   The remaining causes of inconsistency are lag in Sky-           Because bloom filters are in-memory on the TAO host, they
bridge’s replication layer, missing heartbeats from TAO’s          incur almost no additional latency on the request handling
write path and errors talking to Skybridge. Missing heartbeats     critical path. Including requests to Skybridge, TAO is able to
from TAO writers is outside of Skybridge’s control as win-         prove 99.9996% of requests up to date. While a request re-
dows cannot be sent to Skybridge until all outstanding writes      quires a network hop, it is low latency since it is an in-region
are acknowledged by the DB. We are continuing to optimize          request. Figure 7b shows P50, P95 and P99 request latency
the replication layer and Skybridge’s request handling to ad-      to Skybridge. Requests take on the order of milliseconds,
dress the other two sources of inconsistency, although they        meaning TAO can quickly get a staleness answer from Sky-
comprise only a small fraction of fail-open requests.              bridge. Overall, Skybridge is able to provide staleness bound
   TAO’s rate limits are tuned to avoid overloading the up-        checking with low-overhead, with only 0.0004% of requests
stream path and regressing serving latency. Sending too many       needing to go upstream to get fresh data.
requests upstream can regress aggregate latency and cause
an increase in timeouts for products. Because we wanted            5.3    Replication and footprint
bounded staleness enabled by default, we tightened the rate
limits to minimize user impact; the current limits strike a        Figure 7a shows Skybridge’s P99, P99.9 and P99.99 repli-
good balance between strong consistency without user-visible       cation lag, defined as the difference between the Skybridge
performance impact.                                                host’s physical time and the upper bound of the replicated



198   19th USENIX Symposium on Operating Systems Design and Implementation                                  USENIX Association
                                                                     RGD semantics allow Skybridge to be minimal in size
                                                                  while still providing staleness information about recent writes
                                                                  in real time. Skybridge takes full advantage of its core princi-
                                                                  ples (only replicate metadata, known data loss, out-of-order
                                                                  replication, decoupling from Wormhole) to drastically boost
                                                                  TAO’s 2-second bounded staleness up to 99.99998%. It is
                                                                  able to achieve these benefits while helping TAO avoid going
                                                                  upstream for 99.9996% of queries. Skybridge is a reliable and
                                                                  efficient means for boosting TAO’s user experience.

                                                                  6   Limitations and future work
                                                                  Increased retention. Skybridge’s indexes are memory capac-
                                                                  ity bound and only cover several minutes’ worth of replication
       (a) Skybridge replication lag at P99, P99.9 and P99.99
                                                                  lag. Increasing the number of hosts or memory capacity per
                                                                  host would boost retention. Another strategy would be to
                                                                  spill older sections of indexes onto disk (e.g., flash drives).
                                                                  This would allow Skybridge to expand retention into the tens
                                                                  of minutes range but would require care to avoid regressing
                                                                  query latency. Other systems like Memcached [20] are able
                                                                  to support disk storage with minimal regressions, meaning it
                                                                  is likely a feasible solution for Skybridge.
                                                                     Dynamic sharding. TAO statically assigns data to shards,
                                                                  but systems like Cockroach DB [33], BigTable [12], Span-
                                                                  ner [16] and TiDB [28] are able to dynamically reshard data
                                                                  to balance load. Skybridge’s leasing mechanism could be
                                                                  extended to handle dynamic resharding, but would likely re-
                                                                  quire integration with the resharding control plane to safely
       (b) Skybridge latency at P50, P95 and P99 percentiles      coordinate changes.
                                                                     Global secondary indices. Meta uses global secondary
  Figure 7: Skybridge replication and query performance           indices [9] to materialize queries that would require many
                                                                  TAO requests, e.g., inverted indexes on fields. A shuffle ser-
write window. P99 replication lag is stable at around 700ms.      vice ingests updates from Wormhole and generates updates
Even at P99.99, Skybridge is able to replicate windows around     for global secondary index leaf hosts. Skybridge would need
the world within 1.5 seconds outside a few spikes. This real-     to tackle several problems to provide bounded staleness for
time delivery is a combination of small payload size and out-     these indices. First, indexing systems need a way to track
of-order replication. Delivering 99% of the data in ∼700ms        a replication watermark across the large fan-in (potentially
opens up possibilities for sub-2-second staleness bounds. For     millions) of input DB shards. Second, indexing systems must
example, the primary read emulation API could offer tighter       have a way to “repair” a given index when determined to be
latency bounds for use cases that cannot tolerate waiting for 2   missing some writes (similarly to TAO filling from upstream).
seconds, although there may need to be a cap on max through-      We leave both of these problems as future work.
put to avoid hitting rate limits and triggering errors.
                                                                  7   Related Work
   Skybridge requires a relatively small number of servers.
The entire Skybridge ecosystem (Skybridge read and write          Strong consistency on eventually-consistent datastores.
path, regional and global Skylease) uses 0.54% of TAO’s           There are many works that augment eventually-consistent
server footprint. Skybridge is able to retain between 93 and      datastores with stronger consistency. Riak [53] and Cassan-
109 seconds’ worth of recent writes as write traffic fluc-        dra [34] provide opt-in linearizability through quorum reads
tuates throughout the day (Skybridge is memory-capacity           of replicas. Gemini [38] uses logical clocks to direct cross-
bound). A single Skybridge tier pulls information about all       replica reads for serializability. RAMP [5] (and Facebook’s
committed writes from all primary regions, requiring between      RAMP-TAO [13] variant) provides read atomicity for trans-
4.8-7.9GB/s of network bandwidth. Wormhole’s replication          actions. All of these these systems require opting in to heavy-
stream to TAO, meanwhile, requires between 170-300GB/s.           weight protocols, meaning only a small fraction of requests
These numbers demonstrate that Skybridge is lightweight           see improved consistency. Skybridge instead focuses on en-
compared to TAO and Wormhole.                                     abling bounded staleness by default for all reads.



USENIX Association                            19th USENIX Symposium on Operating Systems Design and Implementation           199
   Bounded staleness in datastores. There are a number of                An and Cao describe monotonic consistent caching [2], a
datastores that implement bounded staleness. Azure Cosmos             set of cache eviction policies that take consistency and atom-
DB [41] and Google Spanner [23] implement bounded stale-              icity into consideration. Like Skybridge, their lazy version
ness in multi-region setups. Both datastores provide bounded          selection strategy provides bounded staleness for users. MCC
staleness as an opt-in mechanism and backpressure (i.e., throt-       however is focused on cache evictions and targets lookaside
tle or block) if the local replica is stale. Skybridge instead pro-   cache systems whereas Skybridge targets systems with invali-
vides on-by-default bounded staleness for distributed caches,         dation streams.
which significantly more traffic.                                        Bounded staleness variations. Bailis et al. describe proba-
   C5 [26] and CRAQ [59] propose new replication and con-             bilistic bounded staleness [7], a framework for users to deter-
currency protocols for ensuring DB replicas are kept up to            mine “good enough” staleness for quorum-replicated datas-
date and provide strong consistency. Query Fresh [61], Har-           tores. Similarly, Ouyang et al. propose probabilistic atomicity
monia [68] and IONIA [62] propose using new networking                with well-bounded staleness [48] for providing a probabilistic
and storage technologies to either prevent lag or eliminate           consistency level between eventual and strong consistency.
conflicts between replicas. None of these works address lag           While Skybridge does not guarantee bounded staleness by
in large-scale pub-sub systems such as faulty publishers or           default due to rate limits, it seeks to provide as strong a bound
network congestion like what is present in Meta’s serving             as possible rather than forcing developers to reason about an
stack (and what causes replication lag for TAO).                      acceptable probability of enforcement. Additionally none of
   PolarDB-SCC [64], similarly to Skybridge, uses granular            these works target web-scale workloads like Meta. ACT [65]
staleness information to allow serving consistent reads from          proposes a framework for application-specific consistency,
replicas. The main difference between PolarDB-SCC and                 but requires developers to augment applications to define the
Skybridge are the target workloads – PolarDB-SCC targets              logical consistency units. There are other non-web workloads
strong consistency for a single DB replicated across multiple         that benefit from bounded staleness, e.g., Cipar et al. [17]
machines, whereas Skybridge targets bounded staleness for             propose using bounded staleness for iterative AI algorithms.
millions of DBs spread around the world. There are also
implementation differences such as deriving staleness bounds          8   Conclusion
by reading from the primary vs. using local physical time             In this work we introduced Skybridge, a system for provid-
and waiting for the replica to become up to date vs. refilling        ing bounded staleness for distributed in-memory caches. Sky-
from upstream. Nevertheless, both approaches optimize for             bridge acts as a staleness oracle, providing per-cache item stal-
workloads where most data on replicas is consistent.                  eness information when the DB replication stream is lagging.
   Distributed cache consistency. Web-scale systems [11, 19,          Skybridge is designed to complement the existing replication
34] frequently optimize for low latency and high availability at      stream, optimizing for a flexible and compact replication for-
the expense of consistency. Many systems use caching to scale         mat that enables real-time replication. Skybridge is efficient
out to high throughput with low latency. While some systems           enough to enabled by default on billions of read requests per
use time-based mechanisms like leases [24] or TTLs [63] for           second, while simultaneously requiring only a small fraction
cache consistency, others use invalidations [66] to keep caches       of hosts. Skybridge is also effective, helping boost TAO’s
up to date [11, 18, 56]. Even with invalidation streams, users        2-second consistency up 99.99998%. Skybridge can provide
are subject to replication lag without additional mechanisms          significant developer and user experience benefits for Meta.
for improved consistency. For example, FlightTracker [57]
provides RYW consistency for users within a region by main-           Acknowledgments
taining tickets representing the user’s writes in a lookaside
store. Similarly, Antipode [21] provides cross-datastore causal       We would first like to thank the anonymous SOSP reviewers,
consistency by propagating lineage metadata alongside re-             OSDI reviewers and our shepherd Michael Stumm for their
quests. These systems target different points in the consistency      technical and editorial insights on improving the paper. We
spectrum than Skybridge, but share common motivation in               would also like to thank Audrey Chang, Aaron Kabcenell,
improving user experiences in a scalable manner.                      Shilpa Lawande and Matt Pugh for their thorough review
                                                                      of the paper. Finally we would like to thank Amol Bhave,
   Lu et al. [39] describe measuring consistency in Facebook’s
                                                                      Arthur Charlton, Jinyu Han, Anwesh Joshi, Qiegang Long,
serving stack using both offline and online checking. Their
                                                                      Morgan McLain-Smith, Tushar Pankaj and Ilya Smirnov for
offline framework reports a significantly lower rate of consis-
                                                                      their technical contributions to the systems in this paper.
tency violations vs. Skybridge’s checker – this is likely due to
their checker sampling user read requests (which may happen
significantly later than writes) whereas Skybridge’s checker          References
injects read requests after 2 seconds. Their online checker
injects reads similarly to Skybridge’s checker and reports a           [1] Mustaque Ahamad, Gil Neiger, James E Burns, Prince
significantly higher rate of inconsistency.                                Kohli, and Phillip W Hutto. Causal Memory: Defini-



200    19th USENIX Symposium on Operating Systems Design and Implementation                                     USENIX Association
     tions, Implementation, and Programming. Distributed            Dmitri Petrov, Lovro Puzar, Yee Jiun Song, and Venkat
     Computing, 9(1):37–49, 1995.                                   Venkataramani. TAO: Facebook’s Distributed Data
                                                                    Store for the Social Graph. In 2013 USENIX Annual
 [2] Shuai An and Yang Cao. Making Cache Monotonic                  Technical Conference (USENIX ATC 13), pages 49–60,
     and Consistent. Proceedings of the VLDB Endowment,             San Jose, CA, June 2013. USENIX Association.
     16(4):891–904, December 2022.
                                                               [12] Fay Chang, Jeffrey Dean, Sanjay Ghemawat, Wilson C.
 [3] Apache Software Foundation. Apache Kafka, 2024.                Hsieh, Deborah A. Wallach, Mike Burrows, Tushar
     https://kafka.apache.org/.                                     Chandra, Andrew Fikes, and Robert E. Gruber. Bigtable:
 [4] Özalp Babaoğlu, Keith Marzullo, and Fred B Schnei-            A Distributed Storage System for Structured Data. ACM
     der. A Formalization of Priority Inversion. Real-Time          Transactions on Computer Systems, 26(2), June 2008.
     Systems, 5(4):285–303, 1993.                              [13] Audrey Cheng, Xiao Shi, Lu Pan, Anthony Simpson,
 [5] Peter Bailis, Alan Fekete, Joseph M. Hellerstein, Ali          Neil Wheaton, Shilpa Lawande, Nathan Bronson, Peter
     Ghodsi, and Ion Stoica. Scalable Atomic Visibility with        Bailis, Natacha Crooks, and Ion Stoica. RAMP-TAO:
     RAMP Transactions. In Proceedings of the 2014 ACM              Layering Atomic Transactions on Facebook’s Online
     SIGMOD International Conference on Management of               TAO Data Store. Proceedings of the VLDB Endowment,
     Data, SIGMOD ’14, page 27–38, New York, NY, USA,               14(12):3014–3027, July 2021.
     2014. Association for Computing Machinery.                [14] Edith Cohen, Eran Halperin, and Haim Kaplan. Perfor-
 [6] Peter Bailis and Ali Ghodsi. Eventual Consistency To-          mance Aspects of Distributed Caches Using TTL-Based
     day: Limitations, Extensions, and Beyond: How Can              Consistency. Theoretical Computer Science, 331(1):73–
     Applications Be Built on Eventually Consistent In-             96, 2005. Automata, Languages and Programming.
     frastructure Given No Guarantee of Safety? Queue,         [15] Brian F. Cooper, Raghu Ramakrishnan, Utkarsh Srivas-
     11(3):20–32, March 2013.                                       tava, Adam Silberstein, Philip Bohannon, Hans-Arno
 [7] Peter Bailis, Shivaram Venkataraman, Michael J.                Jacobsen, Nick Puz, Daniel Weaver, and Ramana Yer-
     Franklin, Joseph M. Hellerstein, and Ion Stoica. Prob-         neni. PNUTS: Yahoo!’s hosted data serving platform.
     abilistically bounded staleness for practical partial          Proceedings of the VLDB Endowment, 1(2):1277–1288,
     quorums. Proceedings of the VLDB Endowment,                    August 2008.
     5(8):776–787, April 2012.                                 [16] James C. Corbett, Jeffrey Dean, Michael Epstein, An-
 [8] Mahesh Balakrishnan, Jason Flinn, Chen Shen, Mi-               drew Fikes, Christopher Frost, J. J. Furman, Sanjay
     hir Dharamshi, Ahmed Jafri, Xiao Shi, Santosh Ghosh,           Ghemawat, Andrey Gubarev, Christopher Heiser, Pe-
     Hazem Hassan, Aaryaman Sagar, Rhed Shi, Jingming               ter Hochschild, Wilson Hsieh, Sebastian Kanthak, Eu-
     Liu, Filip Gruszczynski, Xianan Zhang, Huy Hoang,              gene Kogan, Hongyi Li, Alexander Lloyd, Sergey Mel-
     Ahmed Yossef, Francois Richard, and Yee Jiun Song.             nik, David Mwaura, David Nagle, Sean Quinlan, Rajesh
     Virtual Consensus in Delos. In Proceedings of the 14th         Rao, Lindsay Rolig, Yasushi Saito, Michal Szymaniak,
     USENIX Conference on Operating Systems Design and              Christopher Taylor, Ruth Wang, and Dale Woodford.
     Implementation, OSDI’20, USA, 2020. USENIX Asso-               Spanner: Google’s Globally Distributed Database. ACM
     ciation.                                                       Transactions on Computer Systems, 31(3), August 2013.

 [9] Elena Baralis, Stefano Paraboschi, and Ernest Teniente.   [17] Henggang Cui, James Cipar, Qirong Ho, Jin Kyu Kim,
     Materialized Views Selection in a Multidimensional             Seunghak Lee, Abhimanu Kumar, Jinliang Wei, Wei Dai,
     Database. In Proceedings of the 23rd International Con-        Gregory R. Ganger, Phillip B. Gibbons, Garth A. Gib-
     ference on Very Large Data Bases, VLDB ’97, page               son, and Eric P. Xing. Exploiting Bounded Staleness to
     156–165, San Francisco, CA, USA, 1997. Morgan Kauf-            Speed Up Big Data Analytics. In 2014 USENIX Annual
     mann Publishers Inc.                                           Technical Conference (USENIX ATC 14), pages 37–48,
                                                                    Philadelphia, PA, June 2014. USENIX Association.
[10] Ali Basiri, Niosha Behnam, Ruud de Rooij, Lorin
     Hochstein, Luke Kosewski, Justin Reynolds, and Casey      [18] Shirshanka Das, Chavdar Botev, Kapil Surlaker, Bhaskar
     Rosenthal. Chaos Engineering. IEEE Software,                   Ghosh, Balaji Varadarajan, Sunil Nagaraj, David Zhang,
     33(3):35–41, 2016.                                             Lei Gao, Jemiah Westerman, Phanindra Ganti, Boris
                                                                    Shkolnik, Sajid Topiwala, Alexander Pachev, Naveen
[11] Nathan Bronson, Zach Amsden, George Cabrera, Prasad            Somasundaram, and Subbu Subramaniam. All Aboard
     Chakka, Peter Dimov, Hui Ding, Jack Ferris, Anthony            the Databus! LinkedIn’s Scalable Consistent Change
     Giardullo, Sachin Kulkarni, Harry Li, Mark Marchukov,          Data Capture Platform. In Proceedings of the Third



USENIX Association                        19th USENIX Symposium on Operating Systems Design and Implementation        201
      ACM Symposium on Cloud Computing, SoCC ’12, New           [29] Lexiang Huang, Matthew Magnusson, Abishek Ban-
      York, NY, USA, 2012. Association for Computing Ma-             galore Muralikrishna, Salman Estyak, Rebecca Isaacs,
      chinery.                                                       Abutalib Aghayev, Timothy Zhu, and Aleksey Chara-
                                                                     pko. Metastable Failures in the Wild. In 16th USENIX
[19] Giuseppe DeCandia, Deniz Hastorun, Madan Jampani,               Symposium on Operating Systems Design and Imple-
     Gunavardhan Kakulapati, Avinash Lakshman, Alex                  mentation (OSDI 22), pages 73–90, Carlsbad, CA, July
     Pilchin, Swaminathan Sivasubramanian, Peter Vosshall,           2022. USENIX Association.
     and Werner Vogels. Dynamo: Amazon’s Highly Avail-
     able Key-Value Store. In Proceedings of Twenty-First       [30] James J. Hunt, Kiem-Phong Vo, and Walter F. Tichy.
     ACM SIGOPS Symposium on Operating Systems Prin-                 Delta algorithms: An empirical analysis. ACM Trans-
     ciples, SOSP ’07, page 205–220, New York, NY, USA,              actions on Software Engineering and Methodology,
     2007. Association for Computing Machinery.                      7(2):192–214, April 1998.

[20] Dormando. Caching beyond RAM: the case for NVMe,           [31] Marcos Kawazoe Aguilera, Wei Chen, and Sam Toueg.
     June 2018. https://memcached.org/blog/nvm-caching/.             Heartbeat: A Timeout-Free Failure Detector for Qui-
                                                                     escent Reliable Communication. In Distributed Al-
[21] João Ferreira Loff, Daniel Porto, João Garcia, Jonathan         gorithms, pages 126–140, Berlin, Heidelberg, 1997.
     Mace, and Rodrigo Rodrigues. Antipode: Enforcing                Springer Berlin Heidelberg.
     Cross-Service Causal Consistency in Distributed Ap-
     plications. In Proceedings of the 29th Symposium on        [32] Sandeep S Kulkarni, Murat Demirbas, Deepak Madappa,
     Operating Systems Principles, SOSP ’23, page 298–313,           Bharadwaj Avva, and Marcelo Leone. Logical phys-
     New York, NY, USA, 2023. Association for Computing              ical clocks. In Principles of Distributed Systems:
     Machinery.                                                      18th International Conference, OPODIS 2014, Cortina
                                                                     d’Ampezzo, Italy, December 16-19, 2014. Proceedings
[22] Brad Fitzpatrick. Distributed caching with memcached.
                                                                     18, pages 17–32. Springer, 2014.
     Linux Journal, 2004(124):5, 2004.
                                                                [33] Cockroach Labs. Cockroach DB - Load-Based Splitting,
[23] Google Cloud. Timestamp bounds | Spanner, 2024.
                                                                     2020. https://www.cockroachlabs.com/docs/stable/load-
     https://cloud.google.com/spanner/docs/timestamp-
                                                                     based-splitting.
     bounds#bounded_staleness.

[24] Cary G. Gray and David R. Cheriton. Leases: An             [34] Avinash Lakshman and Prashant Malik. Cassandra:
     efficient fault-tolerant mechanism for distributed file         A Decentralized Structured Storage System. SIGOPS
     cache consistency. SIGOPS Operating Systems Review,             Operating Systems Review, 44(2):35–40, April 2010.
     23(5):202–210, November 1989.
                                                                [35] Leslie Lamport. Time, Clocks, and the Ordering of
[25] gRPC Authors. gRPC, 2024. https://grpc.io/.                     Events in a Distributed System. Communications of the
                                                                     ACM, 21(7):558–565, July 1978.
[26] Jeffrey Helt, Abhinav Sharma, Daniel J. Abadi, Wyatt
     Lloyd, and Jose M. Faleiro. C5: Cloned Concurrency         [36] Leslie Lamport. How to Make a Multiprocessor Com-
     Control That Always Keeps Up. Proceedings of the                puter That Correctly Executes Multiprocess Programs.
     VLDB Endowment, 16(1):1–14, September 2022.                     IEEE Transactions on Computers, C-28(9):690–691,
                                                                     1979.
[27] Maurice P. Herlihy and Jeannette M. Wing. Linearizabil-
     ity: A correctness condition for concurrent objects. ACM   [37] Federico Larumbe and Abhishek Mathur. Under the
     Transactions on Programming Languages and Systems,              Hood: Broadcasting Live Video to Millions, December
     12(3):463–492, July 1990.                                       2015. https://engineering.fb.com/2015/12/03/ios/under-
                                                                     the-hood-broadcasting-live-video-to-millions/.
[28] Dongxu Huang, Qi Liu, Qiu Cui, Zhuhe Fang, Xiaoyu
     Ma, Fei Xu, Li Shen, Liu Tang, Yuxing Zhou, Menglong       [38] Cheng Li, Daniel Porto, Allen Clement, Johannes
     Huang, Wan Wei, Cong Liu, Jian Zhang, Jianjun Li,               Gehrke, Nuno Preguiça, and Rodrigo Rodrigues. Mak-
     Xuelian Wu, Lingyu Song, Ruoxi Sun, Shuaipeng Yu,               ing Geo-Replicated Systems Fast as Possible, Consis-
     Lei Zhao, Nicholas Cameron, Liquan Pei, and Xin Tang.           tent when Necessary. In 10th USENIX Symposium on
     TiDB: A Raft-Based HTAP Database. Proceedings                   Operating Systems Design and Implementation (OSDI
     of the VLDB Endowment, 13(12):3072–3084, August                 12), pages 265–278, Hollywood, CA, October 2012.
     2020.                                                           USENIX Association.



202   19th USENIX Symposium on Operating Systems Design and Implementation                            USENIX Association
[39] Haonan Lu, Kaushik Veeraraghavan, Philippe Ajoux,        [50] Nuno Preguiça, Carlos Baquero, and Marc Shapiro.
     Jim Hunt, Yee Jiun Song, Wendy Tobagus, Sanjeev Ku-           Conflict-Free Replicated Data Types (CRDTs), pages
     mar, and Wyatt Lloyd. Existential Consistency: Mea-           1–10. Springer International Publishing, Cham, 2018.
     suring and Understanding Consistency at Facebook. In
     Proceedings of the 25th Symposium on Operating Sys-      [51] Lin Qiao, Kapil Surlaker, Shirshanka Das, Tom Quiggle,
     tems Principles, SOSP ’15, page 295–310, New York,            Bob Schulman, Bhaskar Ghosh, Antony Curtis, Oliver
     NY, USA, 2015. Association for Computing Machinery.           Seeliger, Zhen Zhang, Aditya Auradar, Chris Beaver,
                                                                   Gregory Brandt, Mihir Gandhi, Kishore Gopalakrishna,
[40] Prince Mahajan, Srinath Setty, Sangmin Lee, Allen             Wai Ip, Swaroop Jgadish, Shi Lu, Alexander Pachev,
     Clement, Lorenzo Alvisi, Mike Dahlin, and Michael             Aditya Ramesh, Abraham Sebastian, Rupa Shanbhag,
     Walfish. Depot: Cloud Storage with Minimal Trust. In          Subbu Subramaniam, Yun Sun, Sajid Topiwala, Cuong
     9th USENIX Symposium on Operating Systems Design              Tran, Jemiah Westerman, and David Zhang. On Brew-
     and Implementation (OSDI 10), Vancouver, BC, October          ing Fresh Espresso: LinkedIn’s Distributed Data Serving
     2010. USENIX Association.                                     Platform. In Proceedings of the 2013 ACM SIGMOD
[41] Microsoft, Inc. Consistency Level Choices - Azure             International Conference on Management of Data, SIG-
     Cosmos DB, 2024. https://learn.microsoft.com/en-              MOD ’13, page 1135–1146, New York, NY, USA, 2013.
     us/azure/cosmos-db/consistency-levels#bounded-                Association for Computing Machinery.
     staleness-consistency.                                   [52] Redis, Ltd. Redis, 2024. https://redis.io/.
[42] David L. Mills. RFC0958: Network Time Protocol           [53] Riak.                 Strong      Consistency, 2024.
     (NTP), 1985.                                                  https://docs.riak.com/riak/kv/latest/developing/app-
[43] MySQL. MySQL, 2024. https://www.mysql.com/.                   guide/strong-consistency/index.html.

[44] MySQL.         MySQL :: MySQL 8.0 Reference              [54] Vinay Shankarkumar, Laurent Montini, Tim Frost, and
     Manual :: 20.1.1.1 Source to Replica Replication,             Greg Dowd. RFC 8173: Precision Time Protocol Ver-
     2024. https://dev.mysql.com/doc/refman/8.0/en/group-          sion 2 (PTPv2) Management Information Base, 2017.
     replication-primary-secondary-replication.html.
                                                              [55] Marc Shapiro, Nuno Preguiça, Carlos Baquero, and
[45] Rajesh Nishtala, Hans Fugal, Steven Grimm, Marc               Marek Zawirski. Conflict-Free Replicated Data Types.
     Kwiatkowski, Herman Lee, Harry C. Li, Ryan McElroy,           In Stabilization, Safety, and Security of Distributed Sys-
     Mike Paleczny, Daniel Peek, Paul Saab, David Stafford,        tems, pages 386–400, Berlin, Heidelberg, 2011. Springer
     Tony Tung, and Venkateshwaran Venkataramani. Scal-            Berlin Heidelberg.
     ing Memcache at Facebook. In 10th USENIX Sympo-
     sium on Networked Systems Design and Implementation      [56] Yogeshwer Sharma, Philippe Ajoux, Petchean Ang,
     (NSDI 13), pages 385–398, Lombard, IL, April 2013.            David Callies, Abhishek Choudhary, Laurent Demailly,
     USENIX Association.                                           Thomas Fersch, Liat Atsmon Guz, Andrzej Kotulski,
                                                                   Sachin Kulkarni, Sanjeev Kumar, Harry Li, Jun Li, Ev-
[46] Oleg       Obleukhov      and    Ahmad       Byagowi.         geniy Makeev, Kowshik Prakasam, Robbert Van Re-
     How       Precision     Time    Protocol is       Be-         nesse, Sabyasachi Roy, Pratyush Seth, Yee Jiun Song,
     ing      Deployed     at   Meta, November 2022.               Benjamin Wester, Kaushik Veeraraghavan, and Peter
     https://engineering.fb.com/2022/11/21/production-             Xie. Wormhole: Reliable Pub-Sub to Support Geo-
     engineering/precision-time-protocol-at-meta/.                 replicated Internet Services. In 12th USENIX Sympo-
[47] Oracle.         Range Optimization, April 2025.               sium on Networked Systems Design and Implementation
     https://dev.mysql.com/doc/refman/8.4/en/range-                (NSDI 15), pages 351–366, Oakland, CA, May 2015.
     optimization.html.                                            USENIX Association.

[48] Lingzhi Ouyang, Yu Huang, Hengfeng Wei, and Jian Lu.     [57] Xiao Shi, Scott Pruett, Kevin Doherty, Jinyu Han, Dmitri
     Achieving Probabilistic Atomicity With Well-Bounded           Petrov, Jim Carrig, John Hugg, and Nathan Bronson.
     Staleness and Low Read Latency in Distributed Datas-          FlightTracker: Consistency across Read-Optimized On-
     tores. IEEE Transactions on Parallel and Distributed          line Stores at Facebook. In 14th USENIX Symposium on
     Systems, 32(4):815–829, 2021.                                 Operating Systems Design and Implementation (OSDI
                                                                   20), pages 407–423. USENIX Association, November
[49] Lu Pan.           Cache made consistent.          In          2020.
     Systems@Scale            Summer      2022,      2022.
     https://engineering.fb.com/2022/06/08/core-              [58] Sasu Tarkoma, Christian Esteve Rothenberg, and Eemil
     infra/cache-made-consistent/.                                 Lagerspetz. Theory and Practice of Bloom Filters for



USENIX Association                        19th USENIX Symposium on Operating Systems Design and Implementation          203
      Distributed Systems. IEEE Communications Surveys &
      Tutorials, 14(1):131–155, 2012.
[59] Jeff Terrace and Michael J. Freedman. Object Storage on
     CRAQ: High-Throughput Chain Replication for Read-
     Mostly Workloads. In 2009 USENIX Annual Technical
     Conference (USENIX ATC 09), San Diego, CA, June
     2009. USENIX Association.
[60] The Apache Software Foundation. Apache Zookeeper,
     2020. https://zookeeper.apache.org/.
[61] Tianzheng Wang, Ryan Johnson, and Ippokratis Pandis.
     Query fresh: Log shipping on steroids. Proceedings of
     the VLDB Endowment, 11(4):406–419, December 2017.
[62] Yi Xu, Henry Zhu, Prashant Pandey, Alex Conway, Rob
     Johnson, Aishwarya Ganesan, and Ramnatthan Alagap-
     pan. IONIA: High-Performance Replication for Modern
     Disk-based KV Stores. In 22nd USENIX Conference
     on File and Storage Technologies (FAST 24), pages 225–
     241, Santa Clara, CA, February 2024. USENIX Associ-
     ation.
[63] Juncheng Yang, Yao Yue, and K. V. Rashmi. A Large
     Scale Analysis of Hundreds of In-Memory Cache Clus-
     ters at Twitter. In 14th USENIX Symposium on Oper-
     ating Systems Design and Implementation (OSDI 20),
     pages 191–208. USENIX Association, November 2020.
[64] Xinjun Yang, Yingqiang Zhang, Hao Chen, Chuan Sun,
     Feifei Li, and Wenchao Zhou. PolarDB-SCC: A Cloud-
     Native Database Ensuring Low Latency for Strongly
     Consistent Reads. Proceedings of the VLDB Endow-
     ment, 16(12):3754–3767, August 2023.
[65] Haifeng Yu and Amin Vahdat. Design and Evaluation
     of a Continuous Consistency Model for Replicated Ser-
     vices. In Fourth Symposium on Operating Systems De-
     sign and Implementation (OSDI 2000), San Diego, CA,
     October 2000. USENIX Association.
[66] Haobo Yu, Lee Breslau, and Scott Shenker. A Scalable
     Web Cache Consistency Architecture. In Proceedings
     of the Conference on Applications, Technologies, Archi-
     tectures, and Protocols for Computer Communication,
     SIGCOMM ’99, page 163–174, New York, NY, USA,
     1999. Association for Computing Machinery.
[67] Ping Zhang and Gary Chartrand. Introduction to Graph
     Theory, volume 2. Tata McGraw-Hill New York, 2006.
[68] Hang Zhu, Zhihao Bai, Jialin Li, Ellis Michael, Dan R. K.
     Ports, Ion Stoica, and Xin Jin. Harmonia: Near-Linear
     Scalability for Replicated Storage with In-Network Con-
     flict Detection. Proceedings of the VLDB Endowment,
     13(3):376–389, November 2019.




204   19th USENIX Symposium on Operating Systems Design and Implementation   USENIX Association
