       Paxos Quorum Leases: Fast Reads Without Sacrificing Writes
                                       Iulian Moraru, David G. Andersen, Michael Kaminsky
                                             Carnegie Mellon University and Intel Labs


Abstract                                                                          as the “stable leader” and is responsible for orchestrating all
                                                                                  operations. Absent failures, Multi-Paxos enables operations
This paper describes quorum leases, a new technique that al-                      to commit in a single round trip from the leader to a majority
lows Paxos-based systems to perform reads with high through-                      of replicas. In a Multi-Paxos environment, the most common
put and low latency. Quorum leases do not sacrifice consis-                       read lease optimization is to grant the stable leader a read lease
tency and have only a small impact on system availability and                     on all objects comprising the state—files, key-value pairs or
write latency. Quorum leases allow a majority of replicas to                      relational database records, depending on the application. As
perform strongly consistent local reads, which substantially                      a result, reads can be handled with a single round-trip to the
reduces read latency at those replicas (e.g., by two orders of                    leader, and writes incur no additional slowdown, compared to
magnitude in wide-area scenarios). Previous techniques for                        a Paxos system that does not use leases.
performing local reads in Paxos systems either (a) sacrifice                         Other systems, such as Google’s Megastore [3], instead
consistency; (b) allow only one replica to read locally; or                       grant a read lease to all nodes. This decision aggressively
(c) decrease the availability of the system and increase the                      optimizes for reads at the expense of write latency: all reads
latency of all updates by requiring all replicas to be notified                   can be handled locally at a replica with no inter-replica traffic
synchronously. We describe the design of quorum leases                            whatsoever, but all writes now involve at least one round-trip
and evaluate their benefits compared to previous approaches                       to every replica (instead of just the nearest majority).
through an implementation running in five geo-distributed                            While optimizing the read performance is clearly important
Amazon EC2 datacenters.                                                           (for example, the read-to-write ratio in Google’s advertising
                                                                                  backend F1 is 340:1 [8]), the fact that Megastore’s successor
                                                                                  system Spanner [8] has abandoned the Megastore lease (in
1     Introduction                                                                favor of the leader lease and snapshot reads) suggests that few
Paxos [18] and its numerous variants for state machine repli-                     applications are willing to sacrifice the performance of writes
cation have become increasingly important for large-scale                         in exchange.
Internet services. They extend to span massive datacenters,                          In this paper, we argue that there is an overlooked alterna-
geo-replicate within and across continents, and handle tens                       tive that is a more natural fit to the structure of Paxos: quorum
of billions of operations each day [8]. Many Paxos-based                          leases. In this model, a lease for each object in the system
systems improve performance by using some form of read                            could be granted to different subsets of nodes. The size and
leases, in which one or several replicas can satisfy read re-                     composition of these subsets is selected either based upon
quests locally without having to commit an operation using                        how frequently each replica reads the objects in question (for
the relatively more expensive Paxos protocol. This optimiza-                      best read performance) or based upon their proximity to the
tion obviously improves read throughput and latency, but also                     leader (to improve read performance without slowing write
improves write performance by reducing the total number of                        performance). A particularly suitable size for this subset is
operations that must be handled by the Paxos replicas.                            that of a Paxos quorum: we claim that quorum leases are
   A variety of approaches to read leases have been used in                       a “natural” fit to Paxos because writes in Paxos must, by
practical systems. Most Paxos-based systems use the Multi-                        definition, synchronously contact at least a simple majority
Paxos optimization, in which one node is temporarily selected                     of nodes anyway. Thus, the lease revocation and the Paxos
                                                                                  messages can be combined, often resulting in no additional
Permission to make digital or hard copies of part or all of this work for         overhead or delays for handling a leased object.
personal or classroom use is granted without fee provided that copies are not        Despite the intuitiveness of this approach, implementing
made or distributed for profit or commercial advantage and that copies bear       quorum leases is nontrivial. Compared to approaches in
this notice and the full citation on the first page. Copyrights for third-party
components of this work must be honored. For all other uses, contact the          which the set of nodes with the lease is fixed—either a single
Owner/Author.                                                                     master or all replicas—an implementation of quorum leases
                                                                                  must be able to consistently determine which objects belong
Copyright is held by the Owner/Author(s).                                         to which lease quorum, automatically determine appropriate
SOCC ’14, Nov 03-05 2014, Seattle, WA, USA.
ACM 978-1-4503-3252-1/14/11.                                                      lease durations, and efficiently refresh the leases in a way that
http://dx.doi.org/10.1145/2670979.2671001                                         balances overhead, a high hit rate on leased objects, and rapid
lease expiration in the event of a node or network failure.                          Client
Solving these problems and evaluating the resulting benefits                                    Request(cmd)
is the primary objective of this work.
                                                                                    Replica 1
   Our results (Section 5) suggest that quorum leases present                       (leader)
                                                                                                     Accept(cmd)              Committed(cmd)
an extremely attractive middle ground for practical Paxos-                                                         AcceptOK
                                                                                    Replica 2
based systems: running the YCSB benchmark workload on a
geo-distributed deployment of five-replica Multi-Paxos with
quorum leases, over 80% of reads are handled locally, with no                       Replica 3
wide-area traffic, at each replica, and over 70% of writes have
the smallest client-observed latency attainable in a Multi-                   Figure 1. Steady state interaction in Multi-Paxos. The asyn-
Paxos system, for the given geographic configuration. In                      chronous messages are represented as dashed arrows.
contrast, a single leader lease system achieves 100% of min-
imal latency writes, but only 20% of reads are local (those
performed by the stable Multi-Paxos leader).                                  the command that the replying replica believes may have al-
                                                                              ready been chosen in this instance (in which case the new
                                                                              leader must use that command instead of the newly proposed
2     Overview                                                                one), and also constitutes a promise not to acknowledge older
                                                                              messages from previous leaders. If the new leader receives
We begin with a short overview of the Paxos protocol, and
                                                                              at least bN/2c + 1 acknowledgements for Prepare messages,
then we present the intuition of applying quorum leases to
                                                                              it will proceed to propose its command by sending Accept
Paxos.
                                                                              messages to a majority of peers; if these messages are also ac-
                                                                              knowledged by a majority, the leader commits the command
2.1     Paxos Overview                                                        locally, and then asynchronously notifies all its peers and the
                                                                              client.
The Paxos protocol [19] is used to implement state machine                       Because this canonical mode of operation requires at least
replication by making a set of possibly faulty distributed repli-             two rounds of communication (two round trips) to commit a
cas execute the same commands in the same order. Because                      command—and possibly more in the case of dueling leaders—
each replica behaves like a state machine, all non-faulty repli-              the widely used “Multi-Paxos” optimization [19] designates
cas will transition through the same sequence of states as                    a stable leader replica (i.e., the distinguished proposer). A
a result. To be able to make progress, fewer than half the                    replica becomes the stable leader by running the prepare phase
replicas can be faulty—if N is the total number of replicas,                  for a large (possibly infinite) number of instances at the same
at least bN/2c + 1 must be working correctly. Paxos handles                   time, taking ownership of all of them. In steady state, clients
only non-Byzantine failures: replicas may crash or fail to                    send commands only to the stable leader, which directly pro-
respond indefinitely, but they cannot respond in ways that do                 poses them in the instances it already owns (without running
not conform to the protocol.                                                  the prepare phase first). When a non-leader replica suspects
   The original and most general specification of Paxos uses                  that the leader has failed, it tries to become the new leader by
processors with different roles: leaders, acceptors and learn-                taking ownership of all the instances it believes have not yet
ers. In practice, these roles are collapsed, and a replica plays              been finalized.
multiple roles at the same time. Because this corresponds                        Figure 1 presents a time diagram depicting the message
to practical deployments, we present our design in terms of                   exchanges in a Multi-Paxos system, in steady state.
replicas with identical capabilities.
   The execution of a replicated state machine that uses Paxos
proceeds as a series of independent instances, where the                      2.2      Quorum Leases: Intuition
outcome1 of each Paxos instance is the agreement on which                     Recall that a lease is a time-limited promise from one process
command must be executed by every replica at a particular                     to another to not modify an object during the lease duration.
position in the linear sequence of commands. The voting                       Leases are often coupled with a revocation mechanism: If
process for one instance may happen concurrently with voting                  the lease grantor wishes to modify the object before the lease
processes for other instances, but does not interfere with them.              has expired, it must contact each lease holder and receive
   Upon receiving a command request from a client, a replica                  confirmation that the holder will stop using its local copy of
first tries to become the leader of an instance corresponding                 the object, as shown in Figure 2.
to a command position it believes has not been used. It does                     Quorum leases intertwine the idea of leasing with the nat-
so by sending Prepare messages to at least a majority of repli-               ural set of nodes that must be contacted for a Paxos write,
cas (possibly including itself). A reply to a Prepare contains                bundling the Paxos write operation with the lease revocation.
   1 By FLP [10], it is impossible to guarantee termination, although under   In Paxos, a replica can commit a command only after a ma-
realistic conditions, using timeouts and retransmitting messages ensures      jority quorum—possibly including itself—has acknowledged
termination with high probability.                                            the command. As is apparent from the revocation example
                    Leasing with time expiration

                             Gra
                                 nt   leas
                                             e                                      R1            R3
           Local Writes
                                                     Local Reads
           Not Allowed
                                                       Possible
                                                                                                             R4
                                                                                                         Lease
                                                                                    R2                  Holders
                                                 Lease expires
                                                                                    Grantors           (any size)
           Local Writes                              Local Reads                    (Majority)
             Possible                                Not Allowed
                                                                                                 R5
                      Leasing with early revocation
                               Gra
                                   nt   leas
            Local Writes                         e                  Figure 3. An example lease in which a majority of replicas
                              REV
            Not Allowed               OKE            Local Reads
                                                       Possible     (R1, R2, and R5) have granted leases to two lease holders (R4
                                 OK                                 and R5).
                                                     Local Reads
            Local Writes                             Not Allowed
              Possible
                                                                    3     Design
      Figure 2. Leasing with and without revocation.                We begin by describing the assumptions about the Paxos
                                                                    systems that use quorum leases. We then describe our quorum
                                                                    leases design goals and motivate our design choices.

in Figure 2, if the write quorum includes all nodes that hold       3.1    Assumptions
leases on the object to be modified, then receiving acknowl-
edgements from the write quorum also means that the lease           Communication between the nodes is asynchronous: mes-
has been properly revoked at all replicas.                          sages may be lost or delayed indefinitely. Replicas do not
                                                                    synchronize clocks, but their clock rates are assumed to be
   Of course, this simple design has several complications:         similar, such that a modest guard time can account for clock
(1) if any member of the quorum becomes unavailable, no             drift over a short interval. Failures are non-Byzantine: repli-
command can be committed until the lease expires; and (2)           cas can crash or fail to respond indefinitely, but they will not
replicas outside the quorum cannot perform local reads.             take actions that do not conform to the protocol.
                                                                       We assume that the replicated state consists of multiple
   We solve the first problem by carefully choosing the lease       objects that can be updated and read separately. Clients sub-
duration relative to the round-trip time between the replicas.      mit operations that specify which objects will be updated,
A quorum is notified synchronously only for a set amount of         and queries for reading object values. Multiple updates and
time, after which none of its members can rely on their state       queries can be batched in the same command.
being updated synchronously anymore. If a quorum member
crashes, the system will be unavailable only until the lease        3.2    Design Goals
expires.
                                                                    A quorum lease provides a subset of Paxos replicas the guar-
   To reduce the opportunity cost of not leasing to the full set    antee that they will be notified synchronously of any update to
of nodes, we grant leases on a per-object basis, where each         a particular set of objects before those updates are committed
lease might have a different set of nodes holding it. In this       by any replica. Thus, let R be the set of all replicas in a Paxos
way, while not all nodes can read locally, the nodes generating     group, and let O be the set of all objects replicated by this
the most read traffic for each object can do so. This design is     Paxos group. A quorum lease is a pair (Q, O), where Q ⊆ R
particularly appropriate when the popularity of each object         and O ⊆ O. We call Q the lease quorum for this lease, and
differs substantially across replicas—such as might be the          O the set of granted objects. Every replica r ∈ Q is a lease
case, e.g., in the popularity of users across a geo-distributed     holder.
social network.                                                        Unlike a Paxos quorum, which must include a majority
                                                                    of replicas, a lease quorum can be of any size—e.g., it can
   In conclusion, quorum leases take advantage of the existing      contain fewer than half the replicas.
communication patterns in Paxos to allow replicas to perform           A lease becomes active after a majority of replicas (i.e.,
local reads, without substantially decreasing the availability of   at least bN/2c + 1 replicas, where N = |R|) have granted
the system, and without significantly increasing write latency.     the lease to at least one lease holder. An example of such a
lease is shown in Figure 3, where a majority have granted                     any given moment a lease configuration, and we use Paxos
read leases to two nodes in the set. A replica g grants a lease               to achieve consensus on lease configuration changes. Con-
(Q, O) to a replica r ∈ Q by making a promise:                                ceptually, there is a separate, independent Paxos-replicated
                                                                              state machine for the configuration state; in our implementa-
  1. to notify r synchronously before committing any update                   tion, the configuration Paxos instantiation runs on the same
     to any object in O that g proposes (i.e., g must not com-                nodes as the main Paxos instantiation. Because replica clocks
     mit until it receives a message from r in response to its                are not synchronized, we must establish timing dependencies
     notification), AND                                                       when granting and refreshing leases. We do this through sep-
  2. to acknowledge Accept and Prepare messages2 for up-                      arate communication, independent of the lease configuration
     dates to objects in O only with the condition that the                   change protocol (see below).
     proposer must also notify r synchronously before com-                       Because an unresponsive lease holder will prevent updates
     mitting these updates.                                                   to the leased objects until the lease expires, it is useful for
                                                                              leases to be granted for short periods of time, which are
   To enforce the first condition without extra communication,                refreshed before they expire.
g will include r in any Paxos quorum of acceptors before com-
mitting a Paxos command that it proposes (i.e., for which it
is the leader). To enforce the second condition, every replica                3.3    Design Overview
must attach enough information to its replies to the Paxos                    Although we believe that quorum leases apply to any variant
Accept and Prepare messages from a proposer that the pro-                     of Paxos, we focus for clarity on the most popular: Multi-
poser can determine which replicas the responder has granted                  Paxos.
leases to; the proposer uses this information to synchronously                   While using quorum leases, the replicas of a Paxos system
notify the lease holders before committing. We explain in Sec-                communicate using two categories of messages: (1) The
tion 3.4 how to implement this exchange efficiently by adding                 normal Paxos messages for choosing commands; and (2)
only a short lease identifier to each message. Grantors make                  Lease management messages. We separate these in both
promises using a protocol separate from Paxos, described in                   design and implementation to make it easier to reuse the
Section 3.5                                                                   leases with other Paxos variants, and to modify the lease
   To prevent unbounded periods of unavailability, a promise                  management protocol.
is valid for only a set amount of time, after which it expires.                  Lease management consists of two message sub-types: (2a)
In a correct implementation, a promise must expire at the                     Paxos messages for agreeing on lease configuration changes—
lease holder before expiring at the grantor. When a lease                     what are the quorums and what is the set of object IDs granted
holder has fewer than bN/2c valid promises from different                     to each quorum; and (2b) messages for granting and refresh-
replicas (the holder itself counts as an implicit grantor), the               ing leases.
lease is said to be inactive for that holder.                                    To avoid a dependency on external clock synchronization,
   This mechanism achieves the following: while the lease is                  we set lease timers based upon the causal ordering of messag-
active at a lease holder, that lease holder can assess whether                ing events using a lightweight peer-to-peer protocol described
an update to any of the leased objects is ongoing (i.e., in the               in detail in Section 3.5. This protocol efficiently combines the
process of being committed), and if not, the lease holder can                 computation of lease timers and the granting and refreshing
read the most up-to-date value of that object from its local                  of leases.
store.                                                                           Separating lease configuration and granting conveys sev-
   A quorum lease can be granted to any subset of replicas,                   eral advantages. First, it means that the granting protocol
of any size. However, because updates in Paxos must be ac-                    could be improved independently of the rest of the system
cepted synchronously by a majority of replicas even when not                  to take advantage of, e.g., hardware clock synchronization
using leases, it is advantageous for both latency and availabil-              or stronger assumptions about delays based upon knowledge
ity to make lease quorums be simple majorities (bN/2c + 1                     of the physical connections between machines. Importantly,
out of the total of N replicas). For reduced write latency, it is             the simplicity of the lease granting protocol also makes short
also useful that every lease quorum include the current distin-               lease intervals feasible, which has important benefits for avail-
guished proposer (i.e., the current stable leader, if the Paxos               ability. The lease renewal messages are short and can be
variant used relies on a stable leader—such as, for example,                  piggybacked on existing traffic, as they refer to the current
Multi-Paxos).                                                                 lease configuration through a short configuration ID, instead
   Different Paxos replicas may need to read different sets                   of needing to explicitly describe quorums and granted objects
of objects at different times. We therefore wish to be able                   as the lease configuration messages must.
to update both the set of leased objects, and the lease quo-
rums. We call the totality of quorum leases agreed upon at
                                                                              3.4    Lease Configurations
   2 The condition for acknowledging Prepare messages applies only to

instances where the grantor has already accepted an update for an object in   A lease configuration describes the quorum composition and
O.                                                                            the set of granted objects for all the quorum leases in a Paxos
                                                                                     T1           T3                   T5             t_lease
system at a given time.
   A lease configuration is built incrementally from a se-                                                                                      t_lease
                                                                                                             t_guard
quence of lease configuration changes. Replicas agree on                       R1
                                                                                                        promise                   T5 + t_lease:           T3 + t_guard +
                                                                                    guard
lease configuration changes through Paxos—they partici-                                                                           R1's promise             t_lease: R1's
                                                                                            guard_ACK         promise_ACK         expires at R1 if        promise
pate in Paxos instances that are separate from the normal                                                                         promise_ACK             expires at R1 if
command-choosing instances. The result is a sequence of                                                                                                   no promise_ACK
                                                                               R2
lease configuration changes that all (non-faulty) replicas agree                                                            t_lease              T4 + t_lease:
                                                                                                   t_guard                                       R1's promise
upon. A lease configuration is then simply referred to by the                               T2                T4                                 expires at R2
instance number of the latest change.
   Our basic implementation of quorum leases strives to as-                    Figure 4. When clocks are not synchronized, the lease acti-
sign leases such that: (a) the lease is granted to a simple                    vating procedure uses acknowledgements to safely manage
majority of nodes; and (b) the number of locally-satisfied                     lease intervals. In this diagram, the lease duration is t lease.
read operations is maximized; and (c) the total number of                      A grantor (R1) will only send a promise if its guard is ac-
leases being managed is modest. 3                                              knowledged by the holder (R2). The grantor can thus bound
   It accomplishes this by having replicas track the frequency                 the promise duration even if the holder does not reply to the
of reads and periodically send this information to the current                 promise.
Multi-Paxos leader.4 The leader determines the next lease
configuration such that each object is granted to the majority
quorum that consists of the stable leader and the bN/2c ad-                    replica in the system (thus becoming a grantor). The promise
ditional replicas that have read the object most often; it then                includes the number of the most recent lease configuration
proposes this new configuration—a set of (quorum, object ID                    that the grantor is aware of, the lease duration, and a times-
list) pairs—in one of the special Paxos instances. Because                     tamp. A receiver (i.e., a lease holder) rejects promises for
it is already guaranteed to see all writes by virtue of being                  lease configurations older than the newest one it is aware of.
the Multi-Paxos leader, the stable leader is granted a default                     A lease represents a time during which the grantor will
lease that covers every object not in another lease, and is also               not modify an object without contacting the holder, which
included in every lease quorum.                                                gives the holder permission to read that object locally. For
   There are many possible ways to maintain read statistics.                   safety, the grantor’s “will not modify” window of time must
In our current implementation, when a replica receives a read                  be inclusive of the holder’s “can read locally” window of
request that it cannot service locally, it forwards it to the                  time. For high availability, these times should be as short as
stable Multi-Paxos leader. The leader counts the number of                     possible, so that a failed holder can only block writes for a
forwarded read requests for each object-replica pair. If the                   short period of time. These goals are accomplished by the
number of reads from a given replica is larger than the number                 lease establishing and renewal protocol described in Figures 4
of reads from another replica previously included as a lease                   and 5.
holder for the object, the leader will include the new replica
                                                                                   Before sending a promise, the grantor sends the holder a
as a lease holder in the next lease configuration update.
                                                                               guard message, which the holder must acknowledge. The
   Lease configuration changes happen sufficiently infre-
                                                                               guard specifies a time duration tguard . The subsequent promise
quently that they can be written to a stable log without affect-
                                                                               contains a lease duration tlease . Importantly, the promise is
ing the performance of the system.
                                                                               only valid if received by the holder before tguard has expired.
                                                                               This ensures that even if the holder does not respond to the
3.5     Activating Leases                                                      promise, the grantor knows that the holder will not believe it
                                                                               has a lease subsequent to tguard +tlease seconds after it received
Replicas agree on a lease configuration as described in the                    the guard ACK.
previous section, but the quorum leases that constitute this                       A holder starts a lease timer as soon as it receives a promise.
configuration become active only after being granted as ex-                    The promise expires after its specified lease duration (tlease )
plained in this section.                                                       has passed. A lease holder can consider the lease active while
   The lease configuration covers all leases granted on all                    there are at least bN/2c promises received from different
objects (which may involve many different sets of holder                       peers that have not yet expired.
replicas), and so replicas affirm this configuration in an all-
                                                                                   When renewing active leases, there is no need to send
to-all manner. Every replica sends a promise to every other
                                                                               the guard anymore. The most recent acknowledged promise
   3 Many other optimization goals are both possible and reasonable; explor-   plays the role of the guard: when sending a new promise,
ing them more deeply is an interesting avenue of future work.                  the grantor indicates that it must be received within a time
   4 Uncompressed, this additional traffic amounts to at most 100 KB/sec
                                                                               t 0 +tguard from the most recent received acknowledgment (the
per replica (based on the maximum read rates achieved by our prototype).
However, it is possible to maintain approximate read statistics without any
                                                                               grantor indicates which ACK this was), where t 0 is the time
additional traffic, by having the Multi-Paxos leader count how many times a    elapsed at the grantor since receiving this acknowledgment.
replica that is not a lease holder forwards read requests for each object.     Therefore, the grantor will be able to safely relinquish its
promise after tguard + tlease seconds from sending the renewal.                 are still active (i.e., binding). The quorum of replicas that ac-
   When a grantor becomes aware that a new lease configura-                     cepted W will intersect this quorum of grantors in at least one
tion has been agreed upon while its most recent promises are                    replica grantorR . Because grantorR accepts W , it must be the
still valid, it can take one of two approaches: (1) the grantor                 case that W ’s proposer will learn that there is an active lease
can let its current promises expire, and then send promises                     when grantorR replies to its Accept. Therefore, W ’s proposer
for the new configuration; or (2) the grantor can immediately                   will know that it must notify readerR before committing W
send promises for the current configuration, but while both                     (even if this proposer has not made any promises itself), and
sets of promises are valid, it must abide by the lease rules of                 readerR must execute W before executing any further read,
both the previous and the current configuration.5 For simplic-                  including R. In conclusion, R will observe W .
ity, our current implementation takes the first approach.
   The lease establishing and renewing logic is described in                    Case 2: readerR acquired the lease after W began. This
pseudocode in Figure 5.                                                         case has three sub-cases: (1) If there exists an acceptor that is
                                                                                part of W ’s Paxos quorum and that grants readerR the lease
                                                                                before accepting W , then this scenario reduces to the pre-
3.6     Ensuring Strong Consistency                                             vious case. (2) If readerR itself accepts W before its lease
                                                                                becomes active, it must execute W before R. (3) There exists
Paxos and Multi-Paxos provide strong consistency: opera-
                                                                                an acceptor grantorR that first accepts W and then makes a
tions are strictly serializable (they are both serializable and the
                                                                                promise that readerR takes into account when activating its
temporal order of non-overlapping operations is respected).
                                                                                lease. Because promises contain the index of the most re-
In this section we show that quorum leases maintain this
                                                                                cent accepted Paxos instance at the grantor (therefore at least
consistency guarantee.
                                                                                as recent as W ’s instance at grantorR ), readerR knows that it
   Write-only and composite read-write operations will be                       must wait for commits for all instances up to and including
committed through the normal Paxos protocol and executed                        W ’s instance before replying to any read (including R). These
atomically. They will thus be strictly serializable. Every sim-                 three sub-cases are exhaustive.
ple or composite read-only operation will either be serviced
atomically by a replica that holds leases for all the objects in                   In conclusion, a Paxos system that implements quorum
question, or will be committed through normal Paxos if no                       read leases ensures strict serializability.
such replica exists. Thus, the system ensures serializability.
   To ensure strict serializability, notice that it is necessary
and sufficient to show that given a read-write or write-only
                                                                                3.7    Recovering after a Replica Failure
operation W and a read-only operation R, where the write
set of W intersects with the read set of R, then the system
                                                                                The failure of a lease holder will prevent the system from
observes their temporal order. That is to say (1) if R completes
                                                                                committing updates to any object for which the lease holder
before W begins at any replica, R does not observe W ; and (2)
                                                                                has a lease until enough of the promises made to it expire. The
if W is committed at any replica before R is received by any
                                                                                system can resume committing once a majority of replicas are
replica, R observes W . If R is committed through Paxos, this
                                                                                no longer bound to notify the faulty replica synchronously. In
is true by virtue of the Paxos protocol guarantees. We must
                                                                                practice, the time for which it is blocked is on the order of a
show that the property also holds when R is serviced locally
                                                                                few to ten seconds, depending on the round-trip time between
by some replica.
                                                                                the replicas, as we analyze more carefully in Section 3.8.
   If R completes before W begins, the property holds trivially:
R cannot return a version that does not yet exist anywhere.                        A grantor suspects that a replica may have failed if that
We are therefore left with the case where W was committed                       replica stops replying to its promises or heartbeat messages
before R was received.                                                          (common in many Paxos implementations). After a grace
   The replica that services R locally (we will call it readerR )               period, the grantor will stop trying to renew its promises, and
can only do so if it has a lease for the objects that R refers to.              it will let the ones made so far expire. In the meantime, the
We must therefore be in one of the following situations:                        grantor will contact the Multi-Paxos leader requesting a spe-
                                                                                cial lease configuration update that specifies that the replica
Case 1: readerR acquired the lease before W began. In this                      suspected of failure should be excluded from all quorums it
case, readerR ’s lease was active throughout W ’s Paxos commit                  was part of. Replicas that switch to this new configuration
process. Because the lease is active, by the causal ordering                    no longer need to synchronously notify the possibly-failed
of grantors and holders starting their lease timers, it must be                 replica of updates, and the system can safely resume using
the case that at least bN/2c + 1 replicas (possibly including                   leases.
readerR ) have granted readerR the lease, and their promises                      A replica that rejoins the replica set after a failure must
   5 For example, if an object has been granted to a different quorum, the      wait for twait (Section 3.8) seconds before it can accept and/or
grantor must ensure that it notifies synchronously every replica in the union   propose any commands to ensure that all of its promises have
of the two quorums before committing an update to the object.                   expired.
                        Establishing leases                                                 Renewing leases
  Every replica R becomes a grantor:                                Every replica R that is a grantor:
   1: send Guard(guard duration) to every other replica             14: for every other replica H do
   2: for every GuardACK from any replica H do                      15:   set
   3:   set                                                               grant timerR [H] ← lease duration + guard duration
        grant timerR [H] ← guard duration + lease duration          16:   set t 0 ← the time since the most recent ACK from H
   4:   send Promise(lease duration) to H                           17:   set seqACK ← the sequence number of most recent
   5: for every PromiseReply from any replica H do                        ACK from H
   6:   if reply received before grant timerR [H] expired then      18:   send Promise(lease duration, t 0 , seqACK ) to H
   7:      set grant timerR [H] ← lease duration                    19: for every PromiseReply from any replica H do
                                                                    20:   set grant timerR [H] ← min(grant timerR [H],
  Any replica H, on receiving a Guard(guard duration)                     lease duration)
  from a replica R:
    8: set guard timerH [R] ← guard duration                        Any replica H, on receiving a Promise(lease duration, t 0 ,
    9: reply with a GuardACK                                        seqACK ) from a replica R:
   10: wait for a Promise(lease duration) from R                    20: if Promise received before time t 0 + guard duration
   11: if Promise received before guard timerH [R] expires              since sending ACK with sequence seqACK then
       then                                                         21:    set lease timerH [R] ← lease time
   12:    set lease timerH [R] ← lease duration                     22:    reply with PromiseReply to R
   13:    reply with PromiseReply to R


      {A lease holder H can consider the lease active if at least bN/2c promises from different replicas have yet to expire
      (where N is the total number of replicas).}


                                       Figure 5. Establishing and renewing quorum leases.


3.8    Lease Time and Failures Analysis                             grantor stops issuing lease renewals, the “grace period” tgrace .
                                                                        At the end of the grace period, the grantors will conclude
In this section we analyze the relationship between the inter-      that the node in question has crashed. The grantors can ex-
replica RTTs, the lease duration, and the maximum window            clude the crashed node from the lease configuration state
of write unavailability after a lease holder crashes or becomes     immediately, but they cannot update any objects leased by
temporarily unavailable.                                            the crashed node until they can be certain the crashed (or
   The guard period tguard (Section 3.5) must be larger than        partitioned) node will not read its objects locally. We term
the maximum round-trip time between any two replicas; oth-          this time twait , and it is calculated as follows: in the worst
erwise, the soon-to-be lease holder will reject the subsequent      case, the grantor sent a promise right before the grace period
promise when it arrives. The lease duration tlease , on the         expired, and therefore, by the renewal logic, it must wait
other hand, can be arbitrarily small because grantors can send      for twait = tguard + tlease before it can consider this promise
the lease renewal traffic “blind”—that is, without knowing          expired.
whether or not its previous lease renewals had been received            The maximum period of write unavailability caused by a
successfully. However, for renewals to be consistently suc-         failed lease holder is therefore tgrace + twait = tgrace + tguard +
cessful even when messages are lost and retransmitted, the          tlease . For wide-area setups, this will be on the order of sec-
lease duration should be higher than one RTT, and renewals          onds (approximately 9 seconds for 2 seconds lease and grant
should be triggered at least one RTT before expiration (the         periods and a 5 second grace period.)
larger the lease period and the higher the renewal frequency,           After tgrace expires, but before twait expires, the live replicas
the more likely it is that packet loss will not affect read per-    will initiate a lease configuration change (Section 3.7) so that
formance).                                                          they can resume using leases after twait expires.
   A grantor will stop issuing new leases once it believes a            If the Paxos leader fails, the unavailability time will be the
replica is down, as indicated by a failure to reply to some         maximum of the time to elect a leader and tgrace + twait .
previous message. Such a message loss can only be detected
after (at least) one round-trip time between the grantor and
                                                                    3.9    Multi-object Operations and Batching
grantee. In practice, a grantor will likely wait some additional
time before believing a replica failed. We term this total time,    With quorum leases, different objects may be granted to dif-
from the instant that a replica could have failed to the time its   ferent quorums. Therefore, multi-object update operations
must be synchronously acknowledged by a super-quorum—                                            JP     CA       OR       VA       IRL
the union of all quorums that lease an object updated by the                     Japan          0.4     120      120      180      270
multi-object operation. If such operations are frequent, this                  California               0.4       20       85      150
may affect the performance and the availability of the system.                  Oregon                           0.4       75      170
A possible solution is to track those objects that are frequently               Virginia                                  0.4       92
updated together and ensure they are always granted to the                      Ireland                                             0.4
same quorum.
   A similar problem arises with batching. Batching increases       Table 1. Approximate round-trip times between datacenters
the throughput of Paxos systems by grouping multiple con-           in milliseconds.
current operations in a single Paxos command. If these opera-
tions update objects leased by different quorums, the batch
must be accepted synchronously by the union of all corre-           leases than to quorum or Megastore-leases.6
sponding quorums. To avoid this, replicas must separate
objects granted to different quorums into different batches
before proposing them. Under heavy request load (usually            5     Evaluation
the situation that warrants batching to sustain a high through-
put) there will usually be enough operations of each type for       We run our implementations of quorum leases, classic leader
batching to still be effective.                                     leases and Megastore-type leases, both in a single Amazon
                                                                    EC2 cluster, and in a geo-distributed setting: five Multi-Paxos
                                                                    replicas run in five Amazon EC2 datacenters, located in Vir-
4    Implementation                                                 ginia, Northern California, Oregon, Ireland and Japan. Ten
                                                                    clients are co-located (i.e., in the same datacenter) with each
We implemented quorum leases, classic leader leases and             replica. Replicas and clients run on large Amazon EC2 in-
Megastore-type leases within an existing Multi-Paxos system         stances: two 64-bit virtual cores with two EC2 Compute Units
written in Go (our code is available open source [23]). Be-         each and 7.5 GB of memory. The typical RTT in an EC2 clus-
cause our main focus is on the message and round-trip time          ter is 0.4 ms. The round-trip times between datacenters are
reductions (or increases) due to leasing in the wide area, we       summarized in Table 1.
usually run the system at throughput levels where the imple-
mentation details are not the bottleneck. This reduces the
importance of a specific choice of Paxos framework.                 5.1     The Workload
   Writing Paxos code that uses wall-clock time is standard         In our experiments, we use Multi-Paxos to replicate a key-
practice—because of the necessity of using timeouts, and also       value store. Lacking access to, e.g., user traces from a major
because the leader lease is a common optimization [4, 6, 8].        Internet service, we use the YCSB [7] key-value workload to
Quorum leases are more complex than leader leases, but the          benchmark it.7 Every client in our system proposes Put and
bulk of the implementation deals with aspects that are not          Get operations with keys drawn from either a Zipf distribution
critical for safety, only for performance. The changes to the       (with an exponent of 0.99—the default YCSB implementa-
core Paxos code are relatively straightforward.                     tion of a Zipf generator) or a uniform distribution. For the
   Because a major focus for all leasing strategies is to re-       Zipf distribution, the most popular items differ across dat-
duce latency in the wide area, we implemented as part of our        acenters: the sequence of keys ordered by popularity is a
baseline a latency optimization described by Castro [5]. This       different random permutation for each datacenter. We ran
optimization reduces the commit latency perceived by clients        experiments for two workload ratios of Puts to Gets—1:1 and
that are co-located with a replica other than the Multi-Paxos       1:9—with each client choosing the operation type at random.
stable leader, by having other replicas transmit their Accep-       The skewed Zipf distribution is the ideal workload for quo-
tReplies to both the stable leader and to the replica near the      rum leases because clients in different datacenters will mostly
client. Thus, in the common case, the client does not need to       access different objects. The uniformly distributed workload,
wait the additional time for a message to come back from the        on the other hand, is the worst case scenario for quorum leases
stable leader, which reduces commit time from four one-way
delays to three.                                                         6 Without leases, the near-client replica will be able to commit as soon as

   We implemented this optimization because, while it does          it receives bN/2c AcceptReplies, or bN/2c − 1 AcceptReplies and an Accept
                                                                    from the leader (because it is implicit that the leader must have accepted
not appear to be commonly deployed, it is a straightforward
                                                                    too). With leases, the commit condition is more strict: the replica closest to
algorithmic tweak that reduces commit latency and therefore         the client can commit an operation after receiving Accept or AcceptReplies
more accurately represents the state of the art of the write        messages from all the replicas that hold the lease for the objects updated
latency that can be achieved by a Paxos-based system. This          by the operations (in addition to the previous condition that at least bN/2c
                                                                    replicas in total signal that they have accepted).
optimization benefits all three implementations (leader leases,          7 For ease of integration with our implementation, we implemented a
Megastore-type leases, and quorum leases), but in some cases        custom workload generator that uses the same distributions as YCSB via a
confers slightly more of an advantage to traditional leader         direct translation of the YCSB code into Go.
because an object is equally likely to be accessed by replicas                                                 Fast local reads
that hold a lease for it as by replicas that do not.                                              Japan              81%
                                                                                                California           95%
                                                                                                 Oregon              89%
5.2     Latency                                                                                  Virginia            89%
We ran this workload in the wide area, for one hundred thou-                                     Ireland             81%
sand keys, with fifty simultaneous clients, ten at each of the
five locations.8 Each client sends ten thousand requests to the                 Table 2. Percentages of fast local reads (smaller than 10 ms)
co-located replica, in a closed loop, and measures the latency                  for wide-area quorum leases with 10% writes and 90% reads,
(there are thus 500,000 requests in total). For writes, clients                 Zipf-distributed.
are notified as soon as the operation has been committed, so
we do not wait for it to be executed. This is sufficient to ensure
strict serializability: after receiving the commit notification,                   The Megastore-type lease allows every replica to read any
the client can safely assume that any subsequent operations                     object locally, but requires proposers to notify all other repli-
(proposed by itself or other clients) will be globally ordered                  cas synchronously before committing a write. Typical (95%)
after its write. A read, on the other hand, is executed before                  read latencies are under 10 ms—one to two orders of mag-
notifying the client, so that the read value can be returned                    nitude faster than when communication with a remote data-
with the notification.                                                          center is required. A small percentage of read requests are
   In all experiments, the Multi-Paxos leader is in California.                 delayed by concurrent writes to the same objects: when a
Based on the round-trip times shown in Table 1, California is                   write is ongoing, a replica must delay interfering reads until
the datacenter closest to the center.                                           it can be sure that the write will be committed (i.e., typically
   We set the lease parameters as follows: the lease duration                   until it has received a commit notification). The price of this
was 2 seconds, every grantor renewed the current lease after                    near-optimal read performance is increased write latency, by
500 milliseconds, and the lease configuration was updated                       more than 100 ms in most cases compared to the other leasing
every 10 seconds.                                                               schemes. Each replica must wait for the replica farthest away
   The cumulative distribution of latencies, separate for reads                 to acknowledge an update before committing it and notifying
and writes, is presented in Figure 6 for all three lease strate-                the client. Furthermore, this scheme incurs more risk of un-
gies: quorum leases, single-leader lease, and Megastore-type                    availability for writes: any unresponsive replica will prevent
leases. For quorum leases we run both Zipf-distributed and                      all updates from progressing until the replica’s lease expires.
uniformly distributed workloads. For the skewed workload,                          Quorum leases are a compromise between the two previous
we let the system adapt for 5000 requests from each client                      schemes. Over 80% of reads at every site are performed
before we begin measuring latencies. The ratio of reads to                      locally. Over 70% of updates have the minimum latency
writes is 1:1. This high frequency of writes increases the                      achievable by a geo-distributed Multi-Paxos system, matching
chance that reads will have to wait for concurrent writes to                    that provided by the single leader lease, and 2× to 3× faster
the same object to finish before they can execute, so it con-                   (i.e. 100 to 200 milliseconds lower latency) than writes with
stitutes a disadvantage for quorum leases. For completeness,                    the Megastore approach. Because all replicas are likely to
we also present a summary of quorum lease results for the 9:1                   be part of at least one quorum lease, any replica failure will
read to write ratio in Table 2 (the other two leasing strategies                cause some unavailability for writes. However, this does not
benefit only marginally from this workload change, their read                   affect all writes (as it would for Megastore-type leases), but
latencies being already very low and very high, respectively).                  only writes to the objects granted to the failed replica.
   The single leader lease provides the best write performance                     The worst scenario for quorum leases is that when the
because the leader can always choose the fastest quorum for                     workload is uniformly distributed (also presented in Figure 6).
committing a write. The leader is the sole owner of the lease,                  For that experiment, we set the quorum leases statically, based
so no particular other replica must be notified synchronously                   on geographical proximity: one lease includes the replicas in
when an update occurs. The fastest quorum always includes                       California, Japan and Oregon, the other includes California,
the replica closest to the client, to take full advantage of the                Virginia and Ireland. Each leases half the key space. As
optimization described in Section 4. This low write latency,                    expected, approximately half the request at each location will
however, comes at a cost: only the leader can read locally.                     therefore have the minimum latency, while the other half will
Thus, the single leader lease suffers mean read latency at least                exhibit the worst case latency.
two orders of magnitude larger than that of the competing
strategies.                                                                     5.3    Recovering from a Replica Failure
    8 A larger key space would be advantageous for quorum leases, making it
                                                                                Figure 7 depicts the evolution of write latency over time in
less likely for a key to be accessed by a non-lease-holder. Larger numbers
of concurrent clients would increase only tail read latency, corresponding to
                                                                                an experiment where a non-leader replica fails. Clients co-
those rare situations when a read is delayed by a simultaneous write to the     located with the Paxos leader generate write requests at a
same key.                                                                       steady rate and measure the commit latency. Approximately
                                              California                                                             California
                                100                                                                   100
                                                                   QL                                                                     QL
          Percentage of Reads




                                                                               Percentage of Writes
                                80                         QL-uniform                                 80                          QL-uniform
                                                                   LL                                                                     LL
                                                                   ML                                                                     ML
                                60                                                                    60

                                40                                                                    40

                                20                                                                    20

                                 0                                                                     0
                                      1    10           100             1000                                10    100 200 300            1000
                                          Read Latency [ms]                                                      Write Latency [ms]

                                               Japan                                                                   Japan
                                100                                                                   100
                                                                   QL                                                                     QL
          Percentage of Reads




                                                                               Percentage of Writes
                                80                         QL-uniform                                 80                          QL-uniform
                                                                   LL                                                                     LL
                                                                   ML                                                                     ML
                                60                                                                    60

                                40                                                                    40

                                20                                                                    20

                                 0                                                                     0
                                      1    10           100             1000                                10    100 200 300            1000
                                          Read Latency [ms]                                                      Write Latency [ms]

                                               Oregon                                                                 Oregon
                                100                                                                   100
                                                                   QL                                                                     QL
          Percentage of Reads




                                                                               Percentage of Writes



                                80                         QL-uniform                                 80                          QL-uniform
                                                                   LL                                                                     LL
                                                                   ML                                                                     ML
                                60                                                                    60

                                40                                                                    40

                                20                                                                    20

                                 0                                                                     0
                                      1    10           100             1000                                10    100 200 300            1000
                                          Read Latency [ms]                                                      Write Latency [ms]

                                               Virginia                                                               Virginia
                                100                                                                   100
                                                                   QL                                                                     QL
          Percentage of Reads




                                                                               Percentage of Writes




                                80                         QL-uniform                                 80                          QL-uniform
                                                                   LL                                                                     LL
                                                                   ML                                                                     ML
                                60                                                                    60

                                40                                                                    40

                                20                                                                    20

                                 0                                                                     0
                                      1    10           100             1000                                10    100 200 300            1000
                                          Read Latency [ms]                                                      Write Latency [ms]

                                               Ireland                                                                Ireland
                                100                                                                   100
                                                                   QL                                                                     QL
          Percentage of Reads




                                                                               Percentage of Writes




                                80                         QL-uniform                                 80                          QL-uniform
                                                                   LL                                                                     LL
                                                                   ML                                                                     ML
                                60                                                                    60

                                40                                                                    40

                                20                                                                    20

                                 0                                                                     0
                                      1    10           100             1000                                10    100 200 300            1000
                                          Read Latency [ms]                                                      Write Latency [ms]


Figure 6. CDFs of client-observed latency for each site, with all three lease techniques: quorum lease (QL), single leader lease
(LL), and Megastore-type lease (ML). QL-uniform corresponds to quorum leases for a uniformly-distributed workload. The
read-to-write ratio in these experiments was 1:1. The Multi-Paxos leader is always located in California. Note the log scale on
the X axis.
                                       10000                                                                           5000 updates at a time), the difference in messaging patterns
                                                                                                                       between the three lease implementations is less important, so
                                                                                                                       their write throughputs are approximately the same. Quorum
                        Latency [ms]



                                       1000
                                                                                                                       leases have a 10% lower write throughput because we must
                                                                                                                       separate the updates into per-quorum batches (i.e., only up-
                                        100                                                                            dates leased by the same quorum are batched together). In
                                                                                                                       the local cluster, Megastore leases achieve higher throughput,
                                                                                                                       at the cost of suffering more impact upon node failure: all
                                            10
                                                 0         10000        20000     30000      40000             50000   leases will be stalled when any replica becomes available,
                                                                     Command issue time [ms]                           whereas with a quorum lease, an unresponsive replica will
                                                                                                                       stall only updates for the objects it has leased. In general, our
Figure 7. Latency of write requests over time. Ten seconds                                                             results suggest that the differences between Megastore-type
into the experiment, a non-leader replica fails.                                                                       leases and quorum leases are less important in a local clus-
                                                                                                                       ter than they are in the wide area, if throughput is the main
                                                                                                                       consideration.
                                                                                               7K


                            700000
                                                                             K4




                                                                                             61
                                                                           58




                            600000                          Reads
                                                            Writes
 Throughput [ops/sec]




                            500000
                            400000
                                                         Uniformly
                                                 distributed reads                                                     5.5    Discussion
                                                                                        6K




                            300000
                                                                                                                       These experiments considered single-object operations exclu-
                                                                                       20
                                                           9K

                                                                2K




                                                                                   5K




                                                                                                     5K




                            200000
                                                                                                                       sively. For multi-object operations, if the same objects are
                                                         14

                                                              13




                                                                                  11




                                                                                                    12




                            100000
                                                                                                                       usually accessed together, then they will be leased together,
                                        0
                                                         Leader-lease      Quorum-lease      Megastore-lease           and therefore the corresponding read and write latencies will
                                                                                                                       be similar to those reported here. On the other hand, the write
                                                                                                                       latency of multi-object writes that target objects leased by
Figure 8. Local-area read and write throughput for different                                                           different quorums will approach that of the Megastore leases.
leasing strategies. The “Uniformly-distributed reads” for                                                              If such operations are common, and multi-object reads that
quorum leases corresponds to the situation when clients do                                                             span quorums are also common, the Megastore lease may be
not know which replicas can read locally which objects. Error                                                          more suitable.
bars represent 95% confidence intervals.

                                                                                                                       6     Related Work
ten seconds into the experiment, a non-leader replica that is
a lease holder fails. After a 5 second grace period, and then                                                          Time-based leases have been introduced as a way of improv-
after the expiration of the last lease renewal sent before the                                                         ing the latency of reads while maintaining strong data con-
end of the grace period (i.e., the 2 second lease duration plus                                                        sistency in distributed systems [2, 4, 11, 13, 25]. Quorum
the 2 second guard—as explained Section 3.8) the system                                                                leases preserve this goal in the context of Paxos replicated
recovers availability for writes.                                                                                      state machines.
                                                                                                                          Previous systems have used leases to improve the read
                                                                                                                       performance of Paxos systems in two ways. First, Chan-
5.4                                    Throughput in a Cluster
                                                                                                                       dra et al. [6] proposed the Paxos leader lease (also used in
While our focus is on wide-area replication, we evaluate                                                               Chubby [4] and Spanner [8]), which gives the Multi-Paxos
the throughput of the three leasing strategies in a local-area                                                         leader the ability to perform strongly-consistent reads locally,
cluster as well. Figure 8 compares the maximum read and                                                                for a specified time interval. Second, Megastore [3], a wide-
write throughputs achieved with all three leasing strategies.                                                          area deployment of Paxos, effectively grants every replica in
For quorum leases, we present results for two situations: (1)                                                          the system a lease for every object: a write in Megastore must
different objects are popular at different replicas—i.e., when                                                         synchronously send an invalidate message to every replica
trying to read a certain object, all clients know which replica                                                        that has not accepted the write—so that the remote replica
has a lease for that object; and (2) clients are oblivious of                                                          knows its copy of the object (entity group, in Megastore termi-
lease assignment, and direct their reads uniformly at random                                                           nology) is stale. As shown in our evaluation, the leader lease
across all replicas. In these experiments, the requests were                                                           and the Megastore lease are at opposite extremes of the design
generated open-loop by one client for each replica—this was                                                            spectrum for leases in Paxos: leader leases allow for optimal
sufficient to saturate the system.                                                                                     Multi-Paxos write latency, while Megastore leases sacrifice
   Megastore leases have the best read throughput, narrowly                                                            write latency for optimal read latency at every replica. By
surpassing quorum leases when clients are aware of lease as-                                                           contrast, quorum leases allow for a more fine-grained explo-
signments, and 4× higher than single-leader leases. Because                                                            ration of the design space: leases can be granted to any subset
we use batching to commit writes (the leader batches up to                                                             of replicas and they refer to only a specified portion of the
replicated state, so that different replicas can hold different    ation generally incompatible with Byzantine fault tolerant
leases at the same time. For a geo-distributed replicated state    systems.
machine that observes locality in the popularity of its repli-        Per-client leases are also used in systems such as
cated objects, quorum leases can approximate the benefits          Chubby [4] and Farsite [2] to allow clients to operate on
of both previous approaches simultaneously. This comes at          cached sub-parts of the replicated data (i.e., cached files).
the expense of simplicity, because quorum leases are more             Quorum leases are superficially similar to the notion of
complex to manage.                                                 preferred quorums [1, 9]. The key distinction is that a quorum
   Spanner [8] is a leader-leased, Paxos-based system that         lease constitutes a requirement to synchronously update each
uses TrueTime—accurate clock synchronization clock syn-            lease holder, while preferred quorums are a performance
chronization that requires special hardware—to improve geo-        optimization. In Q/U [1], clients try to communicate with the
distributed read performance. It does so in two ways: first, it    same preferred quorum of replicas every time they want to
improves the management of leader leases, by taking advan-         access a particular object, to increase the chance that those
tage of their synchronized clocks. Second, it can let remote       replicas are up-to-date with all the latest versions. Quorum
replicas read locally by issuing snapshot reads, at timestamps     leases, by contrast, guarantee that replicas in the lease holder
specified by clients. Like a quorum lease, a snapshot read         subset are up-to-date, so clients can read from any one replica
executes locally. Unlike a quorum lease, however, a snapshot       in that subset and not an entire quorum. HQ replication [9]
read may not be up-to-date with respect to updates already         makes only a quorum of replicas execute the full protocol in
committed at other replicas.                                       failure-free situations to reduce the messaging overhead.
   Other systems, such as ZooKeeper [14] and MDCC [17],
allow fast local reads, but make no freshness guarantees (i.e.,
the results may be stale).                                         7    Conclusion
   Previous systems have used Paxos as a mechanism for
                                                                   This paper presented the design and implementation of quo-
granting leases: FaTLease [15] and PaxosLease [24] are repli-
                                                                   rum leases for Paxos. By exploiting the existing messaging
cated state machines that grant leases to individual clients.
                                                                   requirements for Paxos operations, quorum leases provide
They use Paxos to ensure the fault tolerance of the lease-
                                                                   a natural, and therefore efficient, mechanism for allowing
management system, and take advantage of the perishable
                                                                   local reads in a Paxos-replicated system without substantially
property of leases to avoid logging Paxos state to disk. By
                                                                   increasing the delay for write operations to commit. Our eval-
contrast, quorum leases are granted to the replicas themselves
                                                                   uation on geo-distributed replicas in Amazon EC2 datacenters
(not the clients) based on the popularity of replicated objects,
                                                                   shows that these leases work well in practice: More nodes can
and they can be granted to multiple entities (replicas) at the
                                                                   perform reads locally than with simple master-only leases, but
same time instead of just one. Furthermore, unlike FaTLease
                                                                   the write latency increases only modestly and only does so
and PaxosLease, quorum leases specify how leases are en-
                                                                   for typically fewer than 10-20% of operations. We therefore
forced, not just how they are granted.
                                                                   believe that quorum leases are an excellent general-purpose
   Although we have presented quorum leases only in the            leasing mechanism for Paxos-based systems.
context of Multi-Paxos, the most widely used variant of
Paxos, we believe they can also be applied to other Paxos vari-    Acknowledgements: We thank our shepherd Jinyang Li, the
ants [20, 21, 22], as well as systems using similar majority-      anonymous SoCC reviewers, Garth Gibson, Miguel Castro,
consensus replication protocols [14]. Leaderless Paxos             and Greg Ganger. This research was funded in part by Intel
variants, in particular, like Mencius [21] and Egalitarian         via the Intel Science and Technology Center for Cloud Com-
Paxos [22] are a good fit for quorum leases because they do        puting (ISTC-CC), by the National Science Foundation under
not require a single replica to be part of every write quorum—     award CCF-0964474, and by a gift from Google.
this gives more flexibility in choosing lease quorums, and
benefits both write latency and availability, especially with
Egalitarian Paxos, which optimizes for wide- area Paxos com-       References
mit latency.
                                                                    [1] M. Abd-El-Malek, G. R. Ganger, G. R. Goodson, M. K. Re-
   The use of leases in improving the performance of dis-               iter, and J. J. Wylie. Fault-scalable byzantine fault-tolerant
tributed protocols is not restricted to Paxos. Zzyzx [12] is a          services. In Proc. 20th ACM Symposium on Operating Systems
Byzantine fault tolerant system that gives clients exclusive            Principles (SOSP), pages 59–74, Brighton, UK, Oct. 2005.
locks to objects. It does so in order to preclude competing         [2] A. Adya, W. J. Bolosky, M. Castro, G. Cermak, R. Chaiken,
client requests, and thus to reduce the common commit path              J. R. Douceur, Jon, J. Howell, J. R. Lorch, M. Theimer, and R. P.
by one message delay (half a round trip) when compared to               Wattenhofer. FARSITE: Federated, Available, and Reliable
Zyzzyva [16]. Compared to the mechanism in Zzyzx, quorum                Storage for an Incompletely Trusted Environment. In Proc.
leases are granted to the replicas themselves, so multiple (or          5th USENIX OSDI, pages 1–14, Boston, MA, Dec. 2002.
all) clients can benefit from each lease, and solve a different     [3] J. Baker, C. Bond, J. C. Corbett, J. Furman, A. Khorlin, J. Lar-
problem: allowing single-replica consistent reads, an oper-             son, J.-M. Leon, Y. Li, A. Lloyd, and V. Yushprakh. Megastore:
     Providing scalable, highly available storage for interactive ser-         MDCC: Multi-data center consistency. In Proc. 8th ACM
     vices. In Proc. of the Conference on Innovative Data system               European Conference on Computer Systems (EuroSys), Apr.
     Research (CIDR), pages 223–234, 2011.                                     2013.
 [4] M. Burrows. The Chubby lock service for loosely-coupled              [18] L. Lamport. The part-time parliament. ACM Transactions on
     distributed systems. In Proc. 7th USENIX OSDI, Seattle, WA,               Computer Systems, 16(2):133–169, 1998. ISSN 0734-2071.
     Nov. 2006.                                                           [19] L. Lamport. Paxos made simple. ACM SIGACT News, 32(4),
 [5] M. Castro and B. Liskov. Practical byzantine fault tolerance              Dec. 2001.
     and proactive recovery. ACM Transactions on Computer Sys-            [20] L. Lamport.         Fast Paxos.        http://research.
     tems, 20(4):398–461, Nov. 2002.                                           microsoft.com/apps/pubs/default.aspx?
 [6] T. D. Chandra, R. Griesemer, and J. Redstone. Paxos made live:            id=64624, 2006.
     an engineering perspective. In Proc. 26th ACM Symposium              [21] Y. Mao, F. P. Junqueira, and K. Marzullo. Mencius: building
     on Principles of Distributed Computing, PODC ’07, pages                   efficient replicated state machines for WANs. In Proc. 8th
     398–407, New York, NY, USA, 2007. ACM.                                    USENIX OSDI, pages 369–384, San Diego, CA, Dec. 2008.
 [7] B. Cooper, A. Silberstein, E. Tam, R. Ramakrishnan, and              [22] I. Moraru, D. G. Andersen, and M. Kaminsky. There is more
     R. Sears. Benchmarking cloud serving systems with YCSB.                   consensus in egalitarian parliaments. In Proc. 24th ACM Sym-
     In Proc. 1st ACM Symposium on Cloud Computing (SOCC),                     posium on Operating Systems Principles (SOSP), Farmington,
     Indianapolis, IN, June 2010.                                              PA, Nov. 2013.
 [8] J. C. Corbett, J. Dean, M. Epstein, A. Fikes, C. Frost, J. Furman,   [23] I. Moraru, D. G. Andersen, and M. Kaminsky. Quorum
     S. Ghemawat, A. Gubarev, C. Heiser, P. Hochschild, W. Hsieh,              leases code base. https://github.com/efficient/
     S. Kanthak, E. Kogan, H. Li, A. Lloyd, S. Melnik, D. Mwaura,              qlease, Oct. 2014.
     D. Nagle, S. Quinlan, R. Rao, L. Rolig, Y. Saito, M. Szymaniak,      [24] M. Trencseni, A. Gazso, and H. Reinhardt. PaxosLease: Disk-
     C. Taylor, R. Wang, and D. Woodford. Spanner: Google’s                    less Paxos for Leases. http://arxiv.org/pdf/1209.
     globally-distributed database. In Proc. 10th USENIX OSDI.                 4187.pdf, 2012.
     USENIX, 2012.
                                                                          [25] J. Yin, L. Alvisi, M. Dahlin, and C. Lin. Volume leases for
 [9] J. Cowling, D. Myers, B. Liskov, R. Rodrigues, and L. Shrira.             consistency in large-scale systems. IEEE Trans. on Knowl. and
     Hq replication: A hybrid quorum protocol for byzantine fault              Data Eng., 11(4):563–576, July 1999.
     tolerance. In Proc. 7th USENIX OSDI, pages 177–190, Seattle,
     WA, Nov. 2006.
[10] M. J. Fischer, N. A. Lynch, and M. S. Paterson. Impossibility
     of distributed consensus with one faulty process. J. ACM, 32
     (2):374–382, Apr. 1985. ISSN 0004-5411.
[11] C. Gray and D. Cheriton. Leases: an efficient fault-tolerant
     mechanism for distributed file cache consistency. In Proceed-
     ings of the twelfth ACM symposium on Operating systems
     principles, SOSP ’89, pages 202–210, New York, NY, USA,
     1989. ACM.
[12] J. Hendricks, S. Sinnamohideen, G. Ganger, and M. Reiter.
     Zzyzx: Scalable fault tolerance through byzantine locking. In
     Dependable Systems and Networks (DSN), 2010 IEEE/IFIP
     International Conference on, pages 363–372, June 2010.
[13] J. Howard, M. Kazar, S. Menees, D. Nichols, M. Satya-
     narayanan, R. Sidebotham, and M. West. Scale and Perfor-
     mance in a Distributed File System. ACM Transactions on
     Computer Systems, 6(1), Feb. 1988.
[14] P. Hunt, M. Konar, F. P. Junqueira, and B. Reed. ZooKeeper:
     wait-free coordination for internet-scale systems. In Proc.
     USENIX ATC, USENIXATC’10, Berkeley, CA, USA, 2010.
     USENIX Association.
[15] F. Hupfeld, B. Kolbeck, J. Stender, M. Högqvist, T. Cortes,
     J. Marti, and J. Malo. Fatlease: scalable fault-tolerant lease
     negotiation with paxos. In Proceedings of the 17th interna-
     tional symposium on High performance distributed computing,
     HPDC ’08, pages 1–10, 2008.
[16] R. Kotla, L. Alvisi, M. Dahlin, A. Clement, and E. Wong.
     Zyzzyva: speculative byzantine fault tolerance. In Proc. 21st
     ACM Symposium on Operating Systems Principles (SOSP),
     pages 45–58, Stevenson, WA, Oct. 2007.
[17] T. Kraska, G. Pang, M. J. Franklin, S. Madden, and A. Fekete.
